---
phase: 02-content-scraping-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/types.ts
  - src/lib/data/navigation.ts
  - src/lib/data/home.ts
  - src/lib/data/services.ts
  - src/lib/data/software.ts
  - src/lib/data/training.ts
  - src/lib/data/resources.ts
  - src/lib/data/company.ts
  - src/lib/data/careers.ts
  - src/lib/data/contact.ts
autonomous: true
requirements: [FOUND-03]
must_haves:
  truths:
    - "Every page listed in PROJECT.md has a corresponding typed data file in src/lib/data/ with real content from kenexis.com"
    - "TypeScript compilation succeeds with strict mode -- all data files are fully typed with no any types"
    - "No data file contains raw HTML, WordPress shortcodes, or external CDN image URLs"
    - "All 7 software products have complete data entries with names, descriptions, pricing, and feature lists"
    - "All 4 service areas have complete data entries with sub-services, deliverables, and related service cross-references"
    - "All 10 team members have data entries with names, titles, credentials, and local photo paths"
  artifacts:
    - path: "src/lib/data/types.ts"
      provides: "Shared type definitions (NavItem, SocialLink, ContactInfo, SEOMeta)"
      exports: ["NavItem", "SocialLink", "ContactInfo", "SEOMeta"]
    - path: "src/lib/data/navigation.ts"
      provides: "Full site navigation structure with mega menu items"
      exports: ["mainNavigation", "footerNavigation"]
    - path: "src/lib/data/home.ts"
      provides: "Homepage hero, pillars, stats, CTAs"
      exports: ["homeHero", "homePillars", "homeStats"]
    - path: "src/lib/data/services.ts"
      provides: "4 service areas with full detail content"
      exports: ["serviceAreas", "getServiceBySlug"]
    - path: "src/lib/data/software.ts"
      provides: "7 software products with KISS platform overview"
      exports: ["softwareProducts", "kissOverview", "getProductBySlug"]
    - path: "src/lib/data/training.ts"
      provides: "16+ training courses and Kenexis Unlimited"
      exports: ["trainingCourses", "kenexisUnlimited"]
    - path: "src/lib/data/resources.ts"
      provides: "Resource categories, external links"
      exports: ["resourceCategories", "externalLinks"]
    - path: "src/lib/data/company.ts"
      provides: "Company info, 10 team members, 11 representatives"
      exports: ["companyInfo", "teamMembers", "representatives"]
    - path: "src/lib/data/careers.ts"
      provides: "Job listings, company values, benefits"
      exports: ["jobListings", "companyValues", "benefits"]
    - path: "src/lib/data/contact.ts"
      provides: "Contact info, form field definitions, social links"
      exports: ["contactInfo", "formFields", "socialLinks"]
  key_links:
    - from: "src/lib/data/services.ts"
      to: "src/lib/data/types.ts"
      via: "import type { SEOMeta }"
      pattern: "import.*from.*types"
    - from: "src/lib/data/company.ts"
      to: "src/lib/data/types.ts"
      via: "import type { ContactInfo, SocialLink }"
      pattern: "import.*ContactInfo.*SocialLink"
    - from: "src/lib/data/services.ts"
      to: "src/lib/data/services.ts"
      via: "relatedServiceIds cross-references"
      pattern: "relatedServiceIds.*\\[.*\\]"
---

<objective>
Create the complete TypeScript data layer for the Kenexis website -- 10 typed data files containing all real content extracted from kenexis.com during research. Every page's content (home, services, software, training, resources, company, careers, contact) gets a corresponding data file with strict TypeScript interfaces and zero `any` types. All content comes from the research document -- no scraping infrastructure needed.

Purpose: Provides the content foundation that ALL subsequent phases (3-10) build upon. Page components will import directly from these data files.
Output: 10 TypeScript files in `src/lib/data/` containing all site content, fully typed and ready for component consumption.
</objective>

<execution_context>
@C:/Users/geoca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geoca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-scraping-data-layer/02-RESEARCH.md

The research document contains ALL extracted content from kenexis.com: homepage text, service descriptions, software product details (7 products with pricing/features), training courses (16+), team members (10 with credentials), representatives (11 with contact details), career listings (3 jobs), contact info, and resource categories. This is the authoritative content source -- transcribe from research, do not invent.

From Phase 1 (01-01-SUMMARY.md):
- Next.js 16.1.6 with TypeScript strict mode enabled
- Tailwind v4 with brand tokens (navy #0a1628, orange #e87722)
- Path alias: `@/*` maps to `./src/*`
- `src/lib/utils.ts` already exists with `cn()` utility
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared types and core content data files (types, navigation, home, services, software)</name>
  <files>src/lib/data/types.ts, src/lib/data/navigation.ts, src/lib/data/home.ts, src/lib/data/services.ts, src/lib/data/software.ts</files>
  <action>
Create the `src/lib/data/` directory and build 5 files. ALL content comes from the "Scraped Content Reference" section of `02-RESEARCH.md` -- do not invent any text.

**1. `types.ts` -- Shared type definitions:**
- `NavItem` interface: `{ label: string; href: string; description?: string; children?: NavItem[]; icon?: string; }`
- `SocialLink` interface: `{ platform: "linkedin" | "youtube" | "spotify" | "rss"; url: string; label: string; }`
- `ContactInfo` interface: `{ phone: string; email: string; address: string; fax?: string; }`
- `SEOMeta` interface: `{ title: string; description: string; ogImage?: string; }`

**2. `navigation.ts` -- Full site nav structure:**
- Export `mainNavigation: NavItem[]` with top-level items: Services (4 children), Software (7 product children), Training, Resources, About (children: Company, Team, Representatives), Careers, Contact
- Export `footerNavigation` with sitemap links grouped by section, plus social links array and contact info
- All `href` values must use the route structure: `/services`, `/services/pha`, `/software`, `/software/open-pha`, etc.

**3. `home.ts` -- Homepage content:**
- Export `homeHero` object: headline ("Process safety experts: Services, Software, Training"), subheadline (from research), primary CTA ("Explore Services", href: "/services"), secondary CTA ("Try Our Software", href: "/software")
- Export `homePillars` array of 3 pillar objects: Engineering Consulting Services, Software Products, Training -- each with name, description, icon string, href, CTA text
- Export `homeStats` array: founded year (2004/22 years), global representatives (10+ countries), software products (7 integrated products)
- Export `homeFeaturedContent` array: 2-3 featured content items from research ("Safe and Clean Green Energy", "Oil & Gas Petrochemical Changes")
- Export `homeNewsletterCTA` object with title and description text
- Export `homeContactCTA` object with title ("Ready to discuss your safety challenges?"), description, button text, href

**4. `services.ts` -- 4 service areas with full detail:**
- Define `ServiceArea` interface with: id, name, slug, shortDescription, fullDescription, methodology string[], subServices array (name + description), deliverables string[], relatedServiceIds string[], heroImage (use `/images/stock/services-hero.jpg`), icon string, seo: SEOMeta
- Define `SubService` interface: { name: string; description: string; }
- Export `serviceAreas: ServiceArea[]` with all 4 services from research:
  1. PHA (slug: "pha") -- 6 sub-services (HAZID, HAZOP, LOPA, QRA, Auditing, Continuing Engineering Support), deliverables, related: ["sis", "qra"]
  2. QRA (slug: "qra") -- sub-services (FMEA, Facility Siting, Gaussian Modeling, CFD, Continuing Engineering Support), related: ["pha", "sis"]
  3. Fire & Gas Mapping (slug: "fire-gas-mapping") -- 8 core elements, coverage grades, related: ["sis", "pha"]
  4. SIS (slug: "sis") -- sub-services (SIL Verification, Test Plan Dev, FSA, SAT, SRS), related: ["pha", "qra"]
- Export helper: `getServiceBySlug(slug: string): ServiceArea | undefined`

**5. `software.ts` -- 7 products + KISS platform:**
- Define `SoftwareProduct` interface: id, name, slug, trademark (string like "(R)" or "(TM)"), tagline, description, pricing object (type: "yearly"|"free"|"contact", amount?: number, unit?: string, note?: string), features string[], integrations string[], url (kenexis.com product URL for reference), heroImage, icon string, seo: SEOMeta
- Define `KISSOverview` interface: name, description, features string[], loginUrl, storeUrl
- Use `as const` for `PRODUCT_IDS` array
- Export `kissOverview: KISSOverview` with KISS platform details from research
- Export `softwareProducts: SoftwareProduct[]` with all 7 products from research, including exact pricing ($2,300 Open-PHA cloud, $5,700 Vertigo, $1,450 Arbor, $3,500 Bowtie-Q, $1,450 Open-Audit cloud, $13,250 Effigy, $5,000 KISS API)
- Note: Open-PHA and Open-Audit have BOTH free desktop and paid cloud tiers -- model this with a `tiers` array or multiple pricing entries
- Export helper: `getProductBySlug(slug: string): SoftwareProduct | undefined`

**Image paths in data files:** Use paths like `/images/stock/services-hero.jpg`, `/images/stock/software-hero.jpg`. These will be created by Plan 02-02. The paths are just strings -- TypeScript compilation does not validate file existence.

**Anti-patterns to avoid (from research):**
- No raw HTML, WordPress shortcodes, or `&amp;` entities in text content
- No external URLs (kenexis.com/wp-content/) as image paths -- all must start with `/images/`
- No `any` types anywhere -- every field explicitly typed
- No duplicating full objects across files -- use IDs for cross-references (e.g., `relatedServiceIds: ["pha", "sis"]`)
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>5 files exist in src/lib/data/ (types.ts, navigation.ts, home.ts, services.ts, software.ts). All export typed data with real content from kenexis.com. TypeScript compiles with zero errors under strict mode. No `any` types. All 4 service areas and all 7 software products have complete entries.</done>
</task>

<task type="auto">
  <name>Task 2: Create remaining content data files (training, resources, company, careers, contact)</name>
  <files>src/lib/data/training.ts, src/lib/data/resources.ts, src/lib/data/company.ts, src/lib/data/careers.ts, src/lib/data/contact.ts</files>
  <action>
Create 5 more data files in `src/lib/data/`. ALL content from "Scraped Content Reference" in `02-RESEARCH.md`.

**1. `training.ts` -- Training courses + Kenexis Unlimited:**
- Define `TrainingCourse` interface: id, name, description, format ("online-on-demand"), category ("process-safety" | "software-training" | "fire-gas"), relatedProductId?: string (links to software product)
- Export `trainingInfo` object: format description ("Online on-demand with video instruction, downloadable content, quizzes"), pricing ("$950 USD/year per person"), trialInfo ("3-day trial available"), certificates ("Completion certificates")
- Export `trainingCourses: TrainingCourse[]` with all 16 courses from research:
  1. LOPA Participant Training (process-safety)
  2. SIS Overview and Awareness (process-safety)
  3. Safety Requirements Specifications (process-safety)
  4. Security PHA Review (process-safety)
  5. SIL Verification (process-safety)
  6. SIS Maintenance Management Operations (process-safety)
  7. Effigy Software Training (software-training, relatedProductId: "effigy")
  8. Open-PHA Software Training (software-training, relatedProductId: "open-pha")
  9. Vertigo Software Training (software-training, relatedProductId: "vertigo")
  10. Bowtie for Open-PHA (software-training, relatedProductId: "open-pha")
  11. Fire Detector Coverage Calculations (fire-gas)
  12. Introduction to Fire and Gas Mapping (fire-gas)
  13. ISA TR 84.00.07-2019 Changes (fire-gas)
  14. Open-PHA Custom Reporting (software-training, relatedProductId: "open-pha")
  15. Gas Detector Placement (fire-gas)
  16. OpScope Procedure Design & Execution (process-safety)
- Export `kenexisUnlimited` object: name, description, includes (all software + all training), pricingNote ("Contact for quote")

**2. `resources.ts` -- Resource categories and external links:**
- Define `ResourceCategory` interface: id, name, description, href, icon string
- Export `resourceCategories: ResourceCategory[]`: News (/news), Recorded Webinars (/resources/recorded), Newsletter (/newsletter), Tools (/resources/tools), Papers/Articles/Books (/resources/papers), Videos (YouTube), Podcast (Spotify)
- Export `externalLinks` object with YouTube URL, Spotify URL, RSS URL from research
- Export `sampleContent` arrays: 2-3 sample webinar entries and 2-3 sample article entries (use generic titles like "Recent Webinar: Process Safety Trends" since exact individual content was not fully scraped -- keep minimal, the resources page is a category overview)

**3. `company.ts` -- Company info, team, representatives:**
- Import `ContactInfo`, `SocialLink` from `./types`
- Define `TeamMember` interface: id, name, title, credentials string[], photo (local path), isLeadership boolean
- Define `Representative` interface: id, company, contactName (string | null), regions string[], phone, email (string | null), website (string | null)
- Define `CompanyInfo` interface: name, founded, founders string[], headquarters ContactInfo, houstonOffice string, description, mission, industries string[], regulatoryAlignment string[], socialLinks SocialLink[]
- Export `companyInfo: CompanyInfo` with all details from research (founded 2004, 3366 Riverside Drive, etc.)
- Export `teamMembers: TeamMember[]` with all 10 members from research table:
  1. Edward Marszal -- President, PE, photo: /images/team/edward-marszal.png, isLeadership: true
  2. Kevin Mitchell -- Vice President, PE/CFSE, photo: /images/team/kevin-mitchell.png, isLeadership: true
  3. Christopher Weil -- Senior Consultant Houston Regional Manager, photo: /images/team/christopher-weil.png
  4. Sean Cunningham -- Senior Engineer, photo: /images/team/sean-cunningham.png
  5. Austin Bryan -- Senior Engineer, photo: /images/team/austin-bryan.png
  6. Elizabeth Smith -- Senior Engineer PE, photo: /images/team/elizabeth-smith.png
  7. Stephen Gorrell -- Senior Engineer, photo: /images/team/stephen-gorrell.png
  8. Arthur Pierce -- Senior Engineer, photo: /images/team/arthur-pierce.jpg
  9. Mohammed Alzinati -- Senior Engineer Middle East Regional Manager, photo: /images/team/mohammed-alzinati.png
  10. John Applegate -- Senior Engineer, photo: /images/team/john-applegate.jpg
- Export `representatives: Representative[]` with all 11 from research table (Detection & Measurement Systems, Westech, Ino-Tek, Dynamis, Keystone, ExidaSP, SGS ECL, Pinnacle, Anar, Velocis, UIT)
- Export helpers: `getTeamMember(id)`, `getLeadership()`

**4. `careers.ts` -- Job listings and company culture:**
- Define `JobListing` interface: id, title, location, type ("full-time"), description
- Export `jobListings: JobListing[]` with 3 positions from research:
  1. Process Safety Engineer (Senior) -- Columbus OH or Houston TX
  2. Process Safety Engineer (Staff) -- Columbus OH
  3. Senior Electrical Engineer -- location TBD
- Export `companyValues: string[]`: ["Safety and Quality", "Integrity and Trust", "Customer Focus and Innovation", "Teamwork and Accountability"]
- Export `benefits: string[]`: ["Competitive salary", "Health, wellness, and income protection", "Retirement with company match", "Paid time off", "Educational reimbursement", "Flexible work arrangements"]
- Export `applicationEmail` = "employment@kenexis.com"

**5. `contact.ts` -- Contact info and form definitions:**
- Import `ContactInfo`, `SocialLink` from `./types`
- Define `FormField` interface: name, label, type ("text"|"email"|"tel"|"textarea"|"select"), required boolean, placeholder?, options?: string[], maxLength?: number
- Export `contactInfo: ContactInfo` = { phone: "+1-614-451-7031", email: "info@Kenexis.com", address: "3366 Riverside Drive, Suite 200, Columbus, Ohio 43221 USA", fax: "+1-614-451-2643" }
- Export `contactFormFields: FormField[]`: Subject (text, required), Message (textarea, required, maxLength 300), First Name (text, required), Last Name (text, required), Email (email, required), Company (text, optional)
- Export `socialLinks: SocialLink[]` with all 4 from research (LinkedIn, YouTube, Spotify, RSS)

**Critical:** Ensure every field has an explicit type. Run `npx tsc --noEmit` to verify zero errors.
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -20 && echo "---FILE CHECK---" && ls -1 src/lib/data/*.ts | wc -l && echo "---NO ANY CHECK---" && grep -rn ": any" src/lib/data/ || echo "No any types found" && echo "---NO HTTP CHECK---" && grep -rn "http" src/lib/data/ | grep -v "linkedin\|youtube\|spotify\|kenexis.com/news\|unsplash\|pexels\|kiss.kenexis\|store.kenexis\|detect-measure\|westech-ind\|ino-tek\|dynamiscursos\|pinnacleinstruments\|anar.com\|velocis-solutions\|uitsolutions\|sgs.com" || echo "No external image URLs found"</automated>
  </verify>
  <done>All 10 data files exist in src/lib/data/. TypeScript compiles with strict mode and zero errors. File count is 10 (types.ts + 9 content files). No `any` types anywhere. No external CDN image URLs (only legitimate external links like LinkedIn, YouTube, Spotify, representative websites). Every content domain from the site has a corresponding data file with real content from kenexis.com.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors (strict mode)
2. `ls src/lib/data/*.ts | wc -l` returns 10
3. `grep -rn ": any" src/lib/data/` returns no results
4. `grep -rn "wp-content" src/lib/data/` returns no results (no WordPress URLs)
5. `grep -rn "\[caption\]\|\[gallery\]\|&amp;" src/lib/data/` returns no results (no WordPress artifacts)
6. Each data file has at least one named export
7. services.ts contains 4 ServiceArea entries
8. software.ts contains 7 SoftwareProduct entries
9. company.ts contains 10 TeamMember entries and 11 Representative entries
10. training.ts contains 16 TrainingCourse entries
</verification>

<success_criteria>
- 10 TypeScript files in src/lib/data/ with all site content
- Zero TypeScript errors under strict mode
- All content matches kenexis.com (from research document) -- nothing invented
- No raw HTML, WordPress artifacts, or external CDN image URLs
- All data files export typed interfaces and data objects
- Helper functions (getServiceBySlug, getProductBySlug, getTeamMember) work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-scraping-data-layer/02-01-SUMMARY.md`
</output>
