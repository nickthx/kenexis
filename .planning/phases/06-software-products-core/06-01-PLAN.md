---
phase: 06-software-products-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/animated-beam.tsx
  - src/components/ui/badge.tsx
  - src/components/sections/software-hero-section.tsx
  - src/components/sections/product-grid-section.tsx
  - src/components/sections/product-ecosystem-section.tsx
  - src/components/sections/product-cta-section.tsx
  - src/app/(marketing)/software/page.tsx
autonomous: true
requirements: [SOFT-01, SOFT-02, SOFT-11]

must_haves:
  truths:
    - "User sees software landing page at /software with KISS platform overview hero"
    - "User sees a grid of all 7 products with icons, names, taglines, pricing badges, and explore CTAs"
    - "User sees an interactive ecosystem visualization with animated beams connecting 7 product nodes to a central KISS hub"
    - "User sees CTAs linking to kiss.kenexis.com (login) and store.kenexis.com (purchase) that open in new tabs"
  artifacts:
    - path: "src/components/ui/animated-beam.tsx"
      provides: "AnimatedBeam component for ecosystem visualization"
      min_lines: 50
    - path: "src/components/ui/badge.tsx"
      provides: "Badge component for pricing tier labels"
    - path: "src/components/sections/software-hero-section.tsx"
      provides: "KISS platform overview hero section"
      min_lines: 30
    - path: "src/components/sections/product-grid-section.tsx"
      provides: "7-product card grid with icons, badges, CTAs"
      min_lines: 50
    - path: "src/components/sections/product-ecosystem-section.tsx"
      provides: "Interactive AnimatedBeam visualization"
      min_lines: 80
    - path: "src/components/sections/product-cta-section.tsx"
      provides: "External CTA section with KISS login + Store links"
      min_lines: 30
    - path: "src/app/(marketing)/software/page.tsx"
      provides: "Software landing page composing all sections"
      exports: ["metadata", "default"]
  key_links:
    - from: "src/app/(marketing)/software/page.tsx"
      to: "src/components/sections/software-hero-section.tsx"
      via: "import SoftwareHeroSection"
      pattern: "import.*SoftwareHeroSection"
    - from: "src/components/sections/product-grid-section.tsx"
      to: "src/lib/data/software.ts"
      via: "SoftwareProduct type and data"
      pattern: "import.*SoftwareProduct.*software"
    - from: "src/components/sections/product-ecosystem-section.tsx"
      to: "src/components/ui/animated-beam.tsx"
      via: "AnimatedBeam component"
      pattern: "import.*AnimatedBeam"
    - from: "src/components/sections/product-cta-section.tsx"
      to: "src/lib/data/software.ts"
      via: "kissOverview.loginUrl and kissOverview.storeUrl"
      pattern: "kissOverview\\.(loginUrl|storeUrl)"
---

<objective>
Create the software landing page at /software with KISS platform overview, 7-product grid, interactive ecosystem visualization, and external CTA section.

Purpose: Establish the software pillar of the site with a modern SaaS-style landing page that showcases the entire KISS product suite and drives users to explore individual products or go directly to the KISS platform.

Output: Software landing page with 4 sections (hero, product grid, ecosystem visualization, CTAs) plus Badge and AnimatedBeam UI components.
</objective>

<execution_context>
@C:/Users/geoca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geoca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-software-products-core/06-RESEARCH.md
@src/lib/data/software.ts
@src/lib/navigation-utils.ts
@src/components/sections/services-hero-section.tsx
@src/components/sections/services-grid-section.tsx
@src/app/(marketing)/services/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/data/software.ts:
```typescript
export interface SoftwareProduct {
  id: ProductId;
  name: string;
  slug: string;
  trademark: string;
  tagline: string;
  description: string;
  pricing: PricingTier[];
  features: string[];
  integrations: string[];
  url: string;
  heroImage: string;
  icon: string;
  seo: SEOMeta;
}

export interface KISSOverview {
  name: string;
  description: string;
  features: string[];
  loginUrl: string;
  storeUrl: string;
}

export interface PricingTier {
  type: "free" | "yearly" | "contact";
  label: string;
  amount?: number;
  unit?: string;
  note?: string;
}

export const kissOverview: KISSOverview;
export const softwareProducts: SoftwareProduct[];
export const softwareSEO: SEOMeta;
```

From src/lib/navigation-utils.ts:
```typescript
export function getNavIcon(iconName?: string): LucideIcon | null;
```

From ~/design-repos/magicui (AnimatedBeam):
```typescript
export interface AnimatedBeamProps {
  className?: string;
  containerRef: RefObject<HTMLElement | null>;
  fromRef: RefObject<HTMLElement | null>;
  toRef: RefObject<HTMLElement | null>;
  curvature?: number;
  reverse?: boolean;
  pathColor?: string;
  pathWidth?: number;
  pathOpacity?: number;
  gradientStartColor?: string;
  gradientStopColor?: string;
  delay?: number;
  duration?: number;
  startXOffset?: number;
  startYOffset?: number;
  endXOffset?: number;
  endYOffset?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Badge component and copy AnimatedBeam, then create software landing page sections</name>
  <files>
    src/components/ui/badge.tsx,
    src/components/ui/animated-beam.tsx,
    src/components/sections/software-hero-section.tsx,
    src/components/sections/product-grid-section.tsx,
    src/components/sections/product-ecosystem-section.tsx,
    src/components/sections/product-cta-section.tsx
  </files>
  <action>
**Step 1: Install Badge and copy AnimatedBeam**
- Run `npx shadcn@latest add badge` to install the Badge component.
- Copy AnimatedBeam from `~/design-repos/magicui/apps/www/registry/magicui/animated-beam.tsx` to `src/components/ui/animated-beam.tsx`. The file already uses `motion/react` and `@/lib/utils` cn() -- both exist in the project. No modifications needed.

**Step 2: Create SoftwareHeroSection** (`src/components/sections/software-hero-section.tsx`)
- "use client" component mirroring ServicesHeroSection pattern.
- Receives `overview: KISSOverview` prop.
- Uses `software-hero.jpg` as background image with dark gradient overlay (same pattern as ServicesHeroSection).
- Displays `overview.name` as h1 title, `overview.description` as subtitle.
- Interior page hero style: `min-h-[300px] lg:h-[400px]`, no `-mt-16`.
- Motion animations: fade-in h1 (delay 0), fade-in p (delay 0.2).

**Step 3: Create ProductGridSection** (`src/components/sections/product-grid-section.tsx`)
- "use client" component mirroring ServicesGridSection pattern.
- Receives `products: SoftwareProduct[]` prop.
- Section heading: "KISS Software Products" with descriptive subtitle.
- `bg-background` section with `py-16 sm:py-24`.
- Grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` for 7 cards (2+2+2+1 pattern on large screens, or consider making the last card span or centering).
- Each card: Icon via `getNavIcon(product.icon)` at 40px with text-accent, product name with trademark via `<sup className="text-xs">{product.trademark}</sup>`, tagline as CardDescription.
- Add Badge for products with free tier: check `product.pricing.some(t => t.type === "free")` and show `<Badge variant="secondary">Free Desktop Version</Badge>`.
- Explore CTA button: `<Button variant="outline" asChild><Link href={/software/${product.slug}}>Explore</Link></Button>` using next-view-transitions Link.
- Motion stagger: containerVariants (staggerChildren: 0.1) + itemVariants (opacity+y) with whileInView trigger.

**Step 4: Create ProductEcosystemSection** (`src/components/sections/product-ecosystem-section.tsx`)
- "use client" component -- MUST have `"use client"` directive (AnimatedBeam requires client-side APIs: refs, useEffect, ResizeObserver).
- Receives `products: SoftwareProduct[]` prop.
- Dark section: `bg-navy-900 text-white` with `py-16 sm:py-24`.
- Heading: "The KISS Ecosystem" with subtitle "Seven integrated modules working together on a single platform".
- Container: `relative min-h-[500px] mx-auto max-w-4xl` with `containerRef`.
- Center node: KISS hub with `absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2`, orange accent bg, rounded-full, w-20 h-20, "KISS" text, z-10.
- 7 product nodes positioned in a circle around the center using CSS transforms. Calculate positions at evenly spaced angles (360/7 = ~51.4 degrees each). Each node: `absolute`, positioned with `top` and `left` percentages, icon via `getNavIcon()`, product name below icon, w-16 h-16 rounded-full bg-white/10 border border-white/20 backdrop-blur.
- Create a ref for each product node using `useRef<HTMLDivElement>(null)` (7 product refs + 1 center ref + 1 container ref).
- Render an `<AnimatedBeam>` for each product connecting `fromRef={productRef}` to `toRef={kissRef}` with `containerRef={containerRef}`. Use `gradientStartColor="#e87722"` (brand orange) and `gradientStopColor="#0a1628"` (brand navy). Stagger `delay` values (0, 0.5, 1, 1.5, ...) for visual effect. Set `pathColor="rgba(255,255,255,0.15)"` for the base path.
- Circular positioning: For each product at index `i`, compute `angle = (i * 2 * Math.PI) / 7 - Math.PI/2` (start from top), then `x = 50 + radius * Math.cos(angle)` and `y = 50 + radius * Math.sin(angle)` as percentages. Use radius ~38 (percentage of container).

**Step 5: Create ProductCTASection** (`src/components/sections/product-cta-section.tsx`)
- "use client" component.
- Receives `overview: KISSOverview` prop.
- Dark or light section with centered content.
- Heading: "Ready to Transform Your Safety Workflow?" or similar.
- Two CTA buttons side by side using flex-wrap gap-4:
  - Primary (accent bg): "Login to KISS" linking to `overview.loginUrl` with `target="_blank" rel="noopener noreferrer"`, ExternalLink icon.
  - Secondary (outline white): "Visit Kenexis Store" linking to `overview.storeUrl` with `target="_blank" rel="noopener noreferrer"`, ExternalLink icon.
- IMPORTANT: Use `<a>` tag with `asChild` pattern for external links, NOT next-view-transitions Link. Use `<Button asChild><a href={...} target="_blank" rel="noopener noreferrer">...</a></Button>`.
- Motion fade-in on heading and buttons.
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 4 section components and 2 UI components created with zero TypeScript errors. Badge and AnimatedBeam available in src/components/ui/.</done>
</task>

<task type="auto">
  <name>Task 2: Create software landing page composing all sections</name>
  <files>src/app/(marketing)/software/page.tsx</files>
  <action>
Create `src/app/(marketing)/software/page.tsx` following the exact pattern from `src/app/(marketing)/services/page.tsx`:

1. Import Metadata type from "next".
2. Import all 4 section components: SoftwareHeroSection, ProductGridSection, ProductEcosystemSection, ProductCTASection.
3. Import data: `softwareProducts`, `kissOverview`, `softwareSEO` from `@/lib/data/software`.
4. Export metadata using `softwareSEO.title`, `softwareSEO.description`, and `softwareSEO.ogImage` for OpenGraph.
5. Default export `SoftwarePage` server component rendering sections in order:
   - `<SoftwareHeroSection overview={kissOverview} />`
   - `<ProductGridSection products={softwareProducts} />`
   - `<ProductEcosystemSection products={softwareProducts} />`
   - `<ProductCTASection overview={kissOverview} />`

Section order follows alternating backgrounds:
- Hero: dark (navy gradient over image)
- Product Grid: light (bg-background)
- Ecosystem: dark (bg-navy-900)
- CTA: could be light or dark -- use `bg-muted/50` for visual variety

This is a server component (no "use client") -- each section handles its own client-side needs internally.
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Software landing page renders at /software with all 4 sections. Build succeeds. SEO metadata set. KISS login and store CTAs open in new tabs.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` serves /software with all 4 visible sections
3. Product grid shows all 7 products with correct icons, trademarks, and badges
4. Ecosystem visualization shows animated beams connecting products to KISS center
5. KISS login and store CTA buttons have `target="_blank"` and `rel="noopener noreferrer"`
</verification>

<success_criteria>
- /software page renders with hero, 7-product grid, ecosystem visualization, and CTA sections
- All 7 products display with correct Lucide icons from getNavIcon()
- Products with free tiers (Open-PHA, Open-Audit) show "Free Desktop Version" badge
- AnimatedBeam renders animated gradient lines from each product to KISS center
- External CTAs link to kiss.kenexis.com and store.kenexis.com, opening in new tabs
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-software-products-core/06-01-SUMMARY.md`
</output>
