---
phase: 06-software-products-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/product-hero-section.tsx
  - src/components/sections/product-features-section.tsx
  - src/components/sections/product-integrations-section.tsx
  - src/components/sections/product-detail-cta-section.tsx
  - src/app/(marketing)/software/[slug]/page.tsx
autonomous: true
requirements: [SOFT-03, SOFT-04, SOFT-11]

must_haves:
  truths:
    - "User can read the Open-PHA product page at /software/open-pha with SaaS-style hero, feature grid, integrations, and CTA"
    - "User can read the Vertigo product page at /software/vertigo with the same SaaS-style structure"
    - "User sees product names with proper trademark symbols on all product pages"
    - "User sees CTAs linking to kiss.kenexis.com and store.kenexis.com that open in new tabs on product detail pages"
  artifacts:
    - path: "src/components/sections/product-hero-section.tsx"
      provides: "SaaS-style product hero with name, trademark, tagline, description, and dual CTAs"
      min_lines: 40
    - path: "src/components/sections/product-features-section.tsx"
      provides: "Feature grid with check icons for product capabilities"
      min_lines: 40
    - path: "src/components/sections/product-integrations-section.tsx"
      provides: "Integrations section showing connected KISS products"
      min_lines: 30
    - path: "src/components/sections/product-detail-cta-section.tsx"
      provides: "Product-specific CTA with pricing info and external links"
      min_lines: 40
    - path: "src/app/(marketing)/software/[slug]/page.tsx"
      provides: "Dynamic product detail page with generateStaticParams and generateMetadata"
      exports: ["generateStaticParams", "generateMetadata", "default"]
  key_links:
    - from: "src/app/(marketing)/software/[slug]/page.tsx"
      to: "src/lib/data/software.ts"
      via: "getProductBySlug and softwareProducts"
      pattern: "getProductBySlug|softwareProducts"
    - from: "src/app/(marketing)/software/[slug]/page.tsx"
      to: "src/components/sections/product-hero-section.tsx"
      via: "import ProductHeroSection"
      pattern: "import.*ProductHeroSection"
    - from: "src/components/sections/product-detail-cta-section.tsx"
      to: "src/lib/data/software.ts"
      via: "kissOverview.loginUrl and kissOverview.storeUrl"
      pattern: "kissOverview\\.(loginUrl|storeUrl)"
---

<objective>
Create reusable SaaS-style product detail section components and the dynamic [slug] route for software product pages, enabling Open-PHA and Vertigo pages.

Purpose: Give each flagship product a dedicated, conversion-focused page with feature grids, integrations, and clear CTAs -- following SaaS landing page best practices. The reusable components also serve Phase 7's remaining 5 products.

Output: 4 product detail section components + dynamic route serving all 7 product slugs (Phase 6 focuses on Open-PHA and Vertigo content; the remaining 5 will render with existing data and be refined in Phase 7).
</objective>

<execution_context>
@C:/Users/geoca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geoca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-software-products-core/06-RESEARCH.md
@src/lib/data/software.ts
@src/lib/navigation-utils.ts
@src/app/(marketing)/services/[slug]/page.tsx
@src/components/sections/service-detail-sections.tsx
@src/components/sections/services-hero-section.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/data/software.ts:
```typescript
export interface SoftwareProduct {
  id: ProductId;
  name: string;
  slug: string;
  trademark: string;
  tagline: string;
  description: string;
  pricing: PricingTier[];
  features: string[];
  integrations: string[];
  url: string;
  heroImage: string;
  icon: string;
  seo: SEOMeta;
}

export interface PricingTier {
  type: "free" | "yearly" | "contact";
  label: string;
  amount?: number;
  unit?: string;
  note?: string;
}

export interface KISSOverview {
  name: string;
  description: string;
  features: string[];
  loginUrl: string;
  storeUrl: string;
}

export function getProductBySlug(slug: string): SoftwareProduct | undefined;
export const softwareProducts: SoftwareProduct[];
export const kissOverview: KISSOverview;
```

From src/app/(marketing)/services/[slug]/page.tsx (pattern to mirror):
```typescript
export function generateStaticParams() {
  return serviceAreas.map((s) => ({ slug: s.slug }));
}

export async function generateMetadata({
  params,
}: {
  params: Promise<{ slug: string }>;
}): Promise<Metadata> {
  const { slug } = await params;
  // ...
}

export default async function ServiceDetailPage({
  params,
}: {
  params: Promise<{ slug: string }>;
}) {
  const { slug } = await params;
  // ...
}
```

From src/lib/navigation-utils.ts:
```typescript
export function getNavIcon(iconName?: string): LucideIcon | null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create product detail section components</name>
  <files>
    src/components/sections/product-hero-section.tsx,
    src/components/sections/product-features-section.tsx,
    src/components/sections/product-integrations-section.tsx,
    src/components/sections/product-detail-cta-section.tsx
  </files>
  <action>
Create 4 reusable section components following the service-detail-sections.tsx pattern. Each is a "use client" component with motion animations.

**ProductHeroSection** (`src/components/sections/product-hero-section.tsx`)
- Props: `product: SoftwareProduct`.
- Mirrors ServicesHeroSection pattern: Image background with dark gradient overlay.
- Uses `product.heroImage` as background (via next/image, fill, object-cover).
- Interior page hero: `min-h-[300px] lg:h-[400px]`, no `-mt-16`.
- Content (centered, white text):
  - Product icon via `getNavIcon(product.icon)` rendered large (48px) in accent color, centered above title.
  - h1: `{product.name}` with `<sup className="text-xs font-normal">{product.trademark}</sup>` -- MUST include trademark symbol.
  - Tagline as subtitle paragraph in navy-200 color.
- Motion: fade-in on icon (delay 0), h1 (delay 0.1), tagline (delay 0.2).

**ProductFeaturesSection** (`src/components/sections/product-features-section.tsx`)
- Props: `features: string[]`, `productName: string`.
- `bg-background` section with `py-16 sm:py-24`.
- Heading: "{productName} Features" centered, text-3xl font-bold.
- Grid: `grid-cols-1 md:grid-cols-2 gap-4 max-w-3xl mx-auto`.
- Each feature: flex row with `<Check>` icon (text-accent, 20px, strokeWidth 2.5) and feature text (text-lg text-muted-foreground).
- Motion stagger: containerVariants (staggerChildren: 0.08) + itemVariants (opacity+y) with whileInView.
- Follow the exact pattern from ServiceDeliverablesSection but with light background.

**ProductIntegrationsSection** (`src/components/sections/product-integrations-section.tsx`)
- Props: `integrations: string[]`, `productName: string`.
- `bg-muted/50` section with `py-16 sm:py-24`.
- Heading: "Works With" or "Integrations" centered.
- Subtitle: "{productName} integrates seamlessly with these KISS platform modules".
- Display integration names as a flex-wrap row of styled pills/cards centered: each integration in a rounded card with its icon (look up via `getNavIcon()` using a mapping from product name to icon string -- build a local lookup from `softwareProducts` array by matching name to integration string), name text, and a light border.
- Import `softwareProducts` to resolve integration names to their icon strings: `const integrationProduct = softwareProducts.find(p => p.name === integrationName)` then `getNavIcon(integrationProduct?.icon)`.
- Motion: fade-in stagger on the integration pills.

**ProductDetailCTASection** (`src/components/sections/product-detail-cta-section.tsx`)
- Props: `product: SoftwareProduct`, `overview: KISSOverview`.
- `bg-navy-900 text-white` section with `py-16 sm:py-24`.
- Content centered, max-w-3xl:
  - Heading: "Get Started with {product.name}{product.trademark}"
  - Display pricing info: iterate `product.pricing` -- for each tier show label, amount (formatted with $ and commas if present), unit, and note. Use a flex row of pricing cards.
  - For "free" tiers: prominent "Download Free" text. For "yearly" tiers: "$X,XXX/year" formatted pricing.
  - Two CTA buttons (same pattern as ProductCTASection from plan 06-01):
    - Primary: "Try on KISS Platform" linking to `overview.loginUrl`, `target="_blank" rel="noopener noreferrer"`, ExternalLink icon.
    - Secondary: "Visit Kenexis Store" linking to `overview.storeUrl`, `target="_blank" rel="noopener noreferrer"`, ExternalLink icon.
  - IMPORTANT: Use `<a>` with `asChild` for external links, NOT next-view-transitions Link.
- Motion: fade-in on heading, pricing, buttons.
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>4 product detail section components created with zero TypeScript errors. Each accepts typed SoftwareProduct props and renders with motion animations.</done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic software product [slug] route</name>
  <files>src/app/(marketing)/software/[slug]/page.tsx</files>
  <action>
Create `src/app/(marketing)/software/[slug]/page.tsx` mirroring the exact pattern from `src/app/(marketing)/services/[slug]/page.tsx`:

1. **Imports:**
   - `type { Metadata }` from "next"
   - `{ notFound }` from "next/navigation"
   - `{ softwareProducts, getProductBySlug, kissOverview }` from `@/lib/data/software`
   - All 4 product section components: ProductHeroSection, ProductFeaturesSection, ProductIntegrationsSection, ProductDetailCTASection
   - `{ ContactCTASection }` from existing `@/components/sections/contact-cta-section` (reuse the existing general contact CTA at the bottom)

2. **generateStaticParams:** Map `softwareProducts` to `{ slug }` objects. This pre-renders ALL 7 product pages at build time.

3. **generateMetadata:** Follow Next.js 16 pattern with `params: Promise<{ slug: string }>`. Await params, call `getProductBySlug(slug)`, return product SEO metadata (title, description, ogImage).

4. **Default export (async server component):** `ProductDetailPage`
   - Await params, get product by slug, call `notFound()` if not found.
   - Render sections in order:
     - `<ProductHeroSection product={product} />` (dark hero)
     - `<ProductFeaturesSection features={product.features} productName={product.name} />` (light bg)
     - `<ProductIntegrationsSection integrations={product.integrations} productName={product.name} />` (muted bg)
     - `<ProductDetailCTASection product={product} overview={kissOverview} />` (dark navy)
     - `<ContactCTASection />` (reuse existing component)

This single route handles all 7 products. Open-PHA at `/software/open-pha` and Vertigo at `/software/vertigo` are the Phase 6 focus, but all 7 render correctly since the data layer is complete.
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>Dynamic product detail route created. /software/open-pha and /software/vertigo render with hero, features, integrations, pricing, and CTAs. All 7 slugs generate static pages at build time. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors, generating static pages for all 7 product slugs
2. /software/open-pha renders with Open-PHA features, integrations (Vertigo, Bowtie-Q, KISS API), and pricing (Free Desktop + $2,300/yr Cloud)
3. /software/vertigo renders with Vertigo features, integrations (Open-PHA, Arbor, KISS API), and pricing ($5,700/yr)
4. Product names display with correct trademark symbols (registered or TM)
5. External CTA buttons have `target="_blank"` and `rel="noopener noreferrer"`
6. Clicking mega menu software product links navigates to correct detail pages
</verification>

<success_criteria>
- /software/open-pha and /software/vertigo render complete SaaS-style product pages
- All 7 product slugs build successfully via generateStaticParams
- Product hero shows icon, name with trademark, and tagline
- Feature grid displays all product features with check icons
- Integrations section shows connected KISS products with icons
- Pricing section displays correct tier information
- CTAs link to kiss.kenexis.com and store.kenexis.com, opening in new tabs
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-software-products-core/06-02-SUMMARY.md`
</output>
