---
phase: 08-training-resources
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/training/page.tsx
  - src/components/sections/training-courses-section.tsx
  - src/components/sections/training-info-section.tsx
  - src/components/sections/kenexis-unlimited-section.tsx
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02

must_haves:
  truths:
    - "User sees a training page with course listings showing names, descriptions, and category grouping"
    - "User sees Kenexis Unlimited subscription in a SaaS-style presentation with included features"
    - "User sees training format, pricing, and certificate information"
  artifacts:
    - path: "src/app/(marketing)/training/page.tsx"
      provides: "Training landing page composed of section components with SEO metadata"
      min_lines: 20
    - path: "src/components/sections/training-courses-section.tsx"
      provides: "Course catalog grid grouped by category with stagger animations"
      min_lines: 40
    - path: "src/components/sections/training-info-section.tsx"
      provides: "Training format, pricing, trial, and certificate information"
      min_lines: 30
    - path: "src/components/sections/kenexis-unlimited-section.tsx"
      provides: "SaaS-style Kenexis Unlimited subscription card with check-mark feature list"
      min_lines: 40
  key_links:
    - from: "src/components/sections/training-courses-section.tsx"
      to: "src/lib/data/training.ts"
      via: "import trainingCourses"
      pattern: "import.*trainingCourses.*from.*training"
    - from: "src/components/sections/kenexis-unlimited-section.tsx"
      to: "src/lib/data/training.ts"
      via: "import kenexisUnlimited"
      pattern: "import.*kenexisUnlimited.*from.*training"
    - from: "src/app/(marketing)/training/page.tsx"
      to: "src/components/sections/"
      via: "imports all training section components"
      pattern: "import.*Section.*from.*sections"
---

<objective>
Build the complete training landing page with course catalog, training information, and Kenexis Unlimited subscription presentation.

Purpose: Delivers the training section of the Kenexis website, allowing users to browse the 15-course catalog organized by category and understand the Kenexis Unlimited subscription offering.
Output: Training page at /training with 3 new section components + reused hero and CTA sections.
</objective>

<execution_context>
@C:/Users/geoca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geoca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-training-resources/08-RESEARCH.md

@src/lib/data/training.ts
@src/components/sections/services-hero-section.tsx
@src/components/sections/contact-cta-section.tsx
@src/components/sections/services-grid-section.tsx
@src/app/(marketing)/services/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/data/training.ts:
```typescript
export interface TrainingCourse {
  id: string;
  name: string;
  description: string;
  format: "online-on-demand";
  category: "process-safety" | "software-training" | "fire-gas";
  relatedProductId?: string;
}

export interface TrainingInfo {
  format: string;
  pricing: string;
  trialInfo: string;
  certificates: string;
}

export interface KenexisUnlimited {
  name: string;
  description: string;
  includes: string[];
  pricingNote: string;
}

export const trainingInfo: TrainingInfo;
export const trainingCourses: TrainingCourse[];  // 15 courses
export const kenexisUnlimited: KenexisUnlimited;
export const trainingSEO: SEOMeta;
```

From src/components/sections/services-hero-section.tsx:
```typescript
interface ServicesHeroSectionProps {
  title?: string;
  subtitle?: string;
  backgroundImage?: string;
}
export function ServicesHeroSection(props: ServicesHeroSectionProps): JSX.Element;
```

From src/components/sections/contact-cta-section.tsx:
```typescript
export function ContactCTASection(): JSX.Element;
```

Established section patterns:
- All section components use `"use client"` directive
- Container: `py-16 sm:py-24` with `mx-auto max-w-7xl px-4`
- Stagger animations: containerVariants + itemVariants with whileInView viewport trigger
- Background alternation: hero(dark) > light > muted > light > dark(CTA)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Tabs and Separator, create training section components</name>
  <files>
    src/components/sections/training-courses-section.tsx
    src/components/sections/training-info-section.tsx
    src/components/sections/kenexis-unlimited-section.tsx
  </files>
  <action>
First install two new shadcn/ui components needed by this phase:
```bash
npx shadcn@latest add tabs separator
```

Then create three section components:

**training-courses-section.tsx** — "use client" component displaying the 15 courses grouped by category:
- Import `trainingCourses` and `TrainingCourse` from `@/lib/data/training`
- Group courses using `reduce()` into categories: "process-safety", "software-training", "fire-gas"
- Define a `categoryLabels` map: `{ "process-safety": "Process Safety", "software-training": "Software Training", "fire-gas": "Fire & Gas" }`
- Define a `categoryIcons` map using Lucide icons: `{ "process-safety": Shield, "software-training": Monitor, "fire-gas": Flame }` (import from lucide-react)
- Render each category as a subsection with icon + heading + description
- Each course renders as a shadcn Card with CardTitle (course name), CardDescription (course description), and a Badge showing "Online On-Demand"
- Use stagger animation pattern: containerVariants/itemVariants with whileInView viewport trigger (match services-grid-section.tsx pattern exactly)
- Grid layout: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` for course cards within each category group
- Section background: `bg-background` (light)
- Each card includes a "Register on KISS" Button that links to `https://kiss.kenexis.com` via `<a target="_blank" rel="noopener noreferrer">` (external link pattern)

**training-info-section.tsx** — "use client" component showing training format, pricing, and certificates:
- Import `trainingInfo` from `@/lib/data/training`
- Render as a 4-column info grid (1 col on mobile, 2 on sm, 4 on lg) with icon + label + value for each field:
  - Clock icon + "Format" + trainingInfo.format
  - DollarSign icon + "Pricing" + trainingInfo.pricing
  - CalendarCheck icon + "Trial" + trainingInfo.trialInfo
  - Award icon + "Certificates" + trainingInfo.certificates
- Each info item gets a motion fade-in with stagger
- Section background: `bg-muted/50` (muted, for alternation)
- Use the standard `py-16 sm:py-24` and `mx-auto max-w-7xl px-4` container

**kenexis-unlimited-section.tsx** — "use client" component for the Kenexis Unlimited subscription:
- Import `kenexisUnlimited` from `@/lib/data/training`
- Dark section: `bg-navy-900 text-white`
- Centered layout with max-w-4xl
- Large heading with kenexisUnlimited.name, description paragraph below
- Feature list using Check icon (from lucide-react) with `text-accent` color for each item in kenexisUnlimited.includes
- pricingNote displayed below the list
- CTA Button: "Contact for Quote" linking to `/contact` using Link from next-view-transitions (internal link)
- Motion fade-in animation on the entire section content
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Three training section components compile without errors and follow established project patterns (stagger animations, alternating backgrounds, proper container sizing)</done>
</task>

<task type="auto">
  <name>Task 2: Compose training page with metadata and section ordering</name>
  <files>src/app/(marketing)/training/page.tsx</files>
  <action>
Create the training landing page at `src/app/(marketing)/training/page.tsx` as a SERVER component (no "use client"):

- Import Metadata type from "next"
- Import trainingSEO from `@/lib/data/training`
- Import all section components:
  - ServicesHeroSection from `@/components/sections/services-hero-section`
  - TrainingCoursesSection from `@/components/sections/training-courses-section`
  - TrainingInfoSection from `@/components/sections/training-info-section`
  - KenexisUnlimitedSection from `@/components/sections/kenexis-unlimited-section`
  - ContactCTASection from `@/components/sections/contact-cta-section`

- Export metadata using trainingSEO (title, description, openGraph) — match the services/page.tsx pattern exactly

- Section order (following engagement funnel with alternating backgrounds):
  1. ServicesHeroSection — title="Process Safety Training Center", subtitle="Online on-demand courses with video instruction, quizzes, and completion certificates", backgroundImage="/images/stock/training-hero.jpg"
  2. TrainingInfoSection — muted bg (format, pricing, trial, certificates overview)
  3. TrainingCoursesSection — light bg (full course catalog grouped by category)
  4. KenexisUnlimitedSection — dark navy bg (SaaS-style subscription pitch)
  5. ContactCTASection — dark CTA (reused from homepage)
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -10 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Training page builds successfully, accessible at /training, displays all 5 sections in correct order with alternating dark/light backgrounds, shows 15 courses grouped into 3 categories, Kenexis Unlimited with feature checklist, and training info grid</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- `npm run dev` serves /training page with all sections visible
- TypeScript compilation passes (no type errors)
- All 15 courses visible grouped into 3 categories
- Kenexis Unlimited section shows 5 included features with check icons
</verification>

<success_criteria>
- Training page at /training renders with hero, info grid, course catalog, Kenexis Unlimited, and contact CTA
- All 15 courses from training.ts display correctly with category grouping
- Kenexis Unlimited presented in SaaS style with check-mark feature list
- External "Register on KISS" links open in new tabs
- Section backgrounds alternate properly (dark hero > muted info > light courses > dark unlimited > dark CTA)
</success_criteria>

<output>
After completion, create `.planning/phases/08-training-resources/08-01-SUMMARY.md`
</output>
