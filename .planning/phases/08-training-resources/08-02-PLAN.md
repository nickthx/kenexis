---
phase: 08-training-resources
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/resources/page.tsx
  - src/components/sections/resource-categories-section.tsx
  - src/components/sections/webinars-section.tsx
  - src/components/sections/papers-section.tsx
  - src/components/sections/podcast-section.tsx
  - src/components/sections/newsletter-section.tsx
autonomous: true
requirements:
  - RES-01
  - RES-02
  - RES-03
  - RES-04
  - RES-05
  - RES-06

must_haves:
  truths:
    - "User sees a centralized resource hub page with all content types organized"
    - "User can view recorded webinar listings with titles and descriptions"
    - "User can access papers, articles, and books listings"
    - "User sees podcast section with episodes linking to Spotify"
    - "User sees links to YouTube channel and RSS feed"
    - "User can register for newsletter from the resources page"
  artifacts:
    - path: "src/app/(marketing)/resources/page.tsx"
      provides: "Resource hub landing page composed of section components with SEO metadata"
      min_lines: 25
    - path: "src/components/sections/resource-categories-section.tsx"
      provides: "Category card grid showing all 7 resource types"
      min_lines: 40
    - path: "src/components/sections/webinars-section.tsx"
      provides: "Webinar listings with titles, descriptions, and YouTube link"
      min_lines: 30
    - path: "src/components/sections/papers-section.tsx"
      provides: "Papers, articles, and books listings"
      min_lines: 30
    - path: "src/components/sections/podcast-section.tsx"
      provides: "Podcast section with Spotify link and YouTube/RSS external links"
      min_lines: 30
  key_links:
    - from: "src/components/sections/resource-categories-section.tsx"
      to: "src/lib/data/resources.ts"
      via: "import resourceCategories"
      pattern: "import.*resourceCategories.*from.*resources"
    - from: "src/components/sections/webinars-section.tsx"
      to: "src/lib/data/resources.ts"
      via: "import sampleWebinars"
      pattern: "import.*sampleWebinars.*from.*resources"
    - from: "src/components/sections/podcast-section.tsx"
      to: "src/lib/data/resources.ts"
      via: "import externalLinks"
      pattern: "import.*externalLinks.*from.*resources"
    - from: "src/app/(marketing)/resources/page.tsx"
      to: "src/components/sections/"
      via: "imports all resource section components and NewsletterSection"
      pattern: "import.*Section.*from.*sections"
    - from: "src/components/sections/newsletter-section.tsx"
      to: "src/lib/data/home.ts"
      via: "accepts optional props with fallback to homeNewsletterCTA"
      pattern: "(homeNewsletterCTA|title.*description.*buttonText)"
---

<objective>
Build the complete resource hub landing page with category overview, webinar listings, papers/articles, podcast section with external links, and newsletter signup.

Purpose: Delivers the resources section allowing users to explore all Kenexis content types (webinars, papers, podcast, tools, videos) from a single centralized hub with prominent external links to YouTube, Spotify, and RSS.
Output: Resources page at /resources with 4 new section components + modified newsletter section for reuse.
</objective>

<execution_context>
@C:/Users/geoca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geoca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-training-resources/08-RESEARCH.md

@src/lib/data/resources.ts
@src/components/sections/services-hero-section.tsx
@src/components/sections/contact-cta-section.tsx
@src/components/sections/newsletter-section.tsx
@src/components/sections/services-grid-section.tsx
@src/app/(marketing)/services/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/data/resources.ts:
```typescript
export interface ResourceCategory {
  id: string;
  name: string;
  description: string;
  href: string;
  icon: string;  // Lucide icon name string
}

export interface SampleContent {
  title: string;
  description: string;
  category: string;
  href: string;
}

export const resourceCategories: ResourceCategory[];  // 7 categories
export const externalLinks: { youtube: string; spotify: string; rss: string };
export const sampleWebinars: SampleContent[];  // 3 items
export const sampleArticles: SampleContent[];  // 2 items
export const resourcesSEO: SEOMeta;
```

From src/components/sections/newsletter-section.tsx:
```typescript
// Currently imports homeNewsletterCTA from home.ts (hardcoded copy)
// MUST be modified to accept optional props for reuse on resources page
export function NewsletterSection(): JSX.Element;
```

From src/components/sections/services-hero-section.tsx:
```typescript
interface ServicesHeroSectionProps {
  title?: string;
  subtitle?: string;
  backgroundImage?: string;
}
export function ServicesHeroSection(props: ServicesHeroSectionProps): JSX.Element;
```

From src/components/sections/contact-cta-section.tsx:
```typescript
export function ContactCTASection(): JSX.Element;
```

Established section patterns:
- All section components use `"use client"` directive
- Container: `py-16 sm:py-24` with `mx-auto max-w-7xl px-4`
- Stagger animations: containerVariants + itemVariants with whileInView viewport trigger
- Background alternation: hero(dark) > light > muted > light > dark
- External links: `<a href="..." target="_blank" rel="noopener noreferrer">` with ExternalLink icon
- Internal links: `Link` from `next-view-transitions`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create resource section components and modify NewsletterSection for reuse</name>
  <files>
    src/components/sections/resource-categories-section.tsx
    src/components/sections/webinars-section.tsx
    src/components/sections/papers-section.tsx
    src/components/sections/podcast-section.tsx
    src/components/sections/newsletter-section.tsx
  </files>
  <action>
Create four new section components and modify one existing component:

**resource-categories-section.tsx** — "use client" component showing all 7 resource categories:
- Import `resourceCategories` from `@/lib/data/resources`
- Create a local `iconMap` mapping icon strings to Lucide components: `{ newspaper: Newspaper, video: Video, mail: Mail, wrench: Wrench, "book-open": BookOpen, "play-circle": PlayCircle, headphones: Headphones }` (import all from lucide-react). Do NOT use getNavIcon — these are section-specific icons, not navigation icons.
- Render as a grid of shadcn Cards: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`
- Each card: icon (40px, text-accent), CardTitle (category name), CardDescription (category description)
- Cards with internal hrefs (starting with "/") use Link from next-view-transitions
- Cards with external hrefs (YouTube, Spotify) use `<a target="_blank" rel="noopener noreferrer">` with an ExternalLink icon indicator
- Stagger animation pattern matching services-grid-section.tsx
- Section background: `bg-background` (light)
- Section heading: "Explore Our Resources" with subheading about comprehensive process safety resources

**webinars-section.tsx** — "use client" component showing recorded webinar listings:
- Import `sampleWebinars` and `externalLinks` from `@/lib/data/resources`
- Section heading: "Recorded Webinars" with description
- Render each webinar as a Card with title, description, and category badge ("Webinar")
- Grid layout: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
- Below cards: a prominent "Watch All on YouTube" Button linking to `externalLinks.youtube` via `<a target="_blank">` with Video icon (satisfies RES-02 + partial RES-05)
- Stagger animations, section background: `bg-muted/50`

**papers-section.tsx** — "use client" component showing papers, articles, and books:
- Import `sampleArticles` from `@/lib/data/resources`
- Section heading: "Papers, Articles & Books" with description
- Render each article as a Card with title, description, and category badge
- Grid layout: `grid grid-cols-1 md:grid-cols-2 gap-6`
- Stagger animations, section background: `bg-background`

**podcast-section.tsx** — "use client" component for the podcast and external media links:
- Import `externalLinks` from `@/lib/data/resources`
- Two-part layout within one section:
  1. Podcast feature: Large card or featured block with Headphones icon, "Kenexis Functional Safety Podcast" heading, description about expert discussions on process safety topics hosted by Ed Marszal, and a "Listen on Spotify" Button linking to `externalLinks.spotify` via `<a target="_blank">`
  2. Additional links row: Cards for YouTube channel (externalLinks.youtube) and RSS feed (externalLinks.rss), each with appropriate icon (PlayCircle, Rss from lucide-react) and `<a target="_blank">` (satisfies RES-04 + RES-05)
- Section background: `bg-navy-900 text-white` (dark, matching Kenexis Unlimited pattern)
- Motion fade-in animation

**Modify newsletter-section.tsx** — Make props-driven for reuse on resources page (satisfies RES-06):
- Add optional props: `title?: string`, `description?: string`, `buttonText?: string`
- Default values fall back to homeNewsletterCTA fields (preserving existing homepage behavior with zero changes to homepage)
- Keep all existing functionality (demo mode, submitted state, styling) unchanged
- This is a minimal change: add 3 optional props to the function signature, use them with `??` fallback to homeNewsletterCTA values
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Four new resource section components and modified NewsletterSection compile without errors. NewsletterSection still works unchanged on homepage (no breaking change). All external links use anchor tags with target="_blank".</done>
</task>

<task type="auto">
  <name>Task 2: Compose resources page with metadata and section ordering</name>
  <files>src/app/(marketing)/resources/page.tsx</files>
  <action>
Create the resource hub landing page at `src/app/(marketing)/resources/page.tsx` as a SERVER component (no "use client"):

- Import Metadata type from "next"
- Import resourcesSEO from `@/lib/data/resources`
- Import all section components:
  - ServicesHeroSection from `@/components/sections/services-hero-section`
  - ResourceCategoriesSection from `@/components/sections/resource-categories-section`
  - WebinarsSection from `@/components/sections/webinars-section`
  - PapersSection from `@/components/sections/papers-section`
  - PodcastSection from `@/components/sections/podcast-section`
  - NewsletterSection from `@/components/sections/newsletter-section`
  - ContactCTASection from `@/components/sections/contact-cta-section`

- Export metadata using resourcesSEO (title, description, openGraph) — match the services/page.tsx pattern exactly

- Section order (following engagement funnel with alternating backgrounds):
  1. ServicesHeroSection — title="Process Safety Resources", subtitle="Webinars, papers, tools, and expert content for safety professionals", backgroundImage="/images/stock/hero-control-room.jpg" (reuse existing stock image per research recommendation — no resources-hero.jpg exists)
  2. ResourceCategoriesSection — light bg (overview grid of all 7 content types)
  3. WebinarsSection — muted bg (recorded webinar listings + YouTube link)
  4. PapersSection — light bg (papers, articles, and books listings)
  5. PodcastSection — dark navy bg (podcast + YouTube + RSS external links)
  6. NewsletterSection — light bg, pass resources-specific props: title="Stay Updated on Process Safety", description="Subscribe to receive monthly safety articles, webinar announcements, and industry insights", buttonText="Subscribe"
  7. ContactCTASection — dark CTA (reused from homepage)
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -10 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Resources page builds successfully, accessible at /resources, displays all 7 sections in correct order with alternating backgrounds, shows 7 resource categories, webinar listings, paper listings, podcast section with Spotify/YouTube/RSS links, and newsletter signup with resources-specific copy</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- `npm run dev` serves /resources page with all sections visible
- TypeScript compilation passes (no type errors)
- Homepage newsletter section still renders correctly (no regression from prop changes)
- All external links (YouTube, Spotify, RSS) open in new tabs
- 7 resource categories display with correct icons
</verification>

<success_criteria>
- Resources page at /resources renders with hero, category grid, webinars, papers, podcast, newsletter, and contact CTA
- All 7 resource categories from resources.ts display with appropriate Lucide icons
- Webinar section shows 3 sample webinars with YouTube channel link
- Papers section shows 2 sample articles
- Podcast section links to Spotify, YouTube channel, and RSS feed (all opening in new tabs)
- Newsletter section displays with resources-specific copy (not homepage copy)
- Homepage newsletter still works unchanged (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/08-training-resources/08-02-SUMMARY.md`
</output>
