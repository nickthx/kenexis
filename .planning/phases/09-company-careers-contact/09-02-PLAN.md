---
phase: 09-company-careers-contact
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/careers/page.tsx
  - src/app/(marketing)/contact/page.tsx
  - src/components/sections/job-listings-section.tsx
  - src/components/sections/company-culture-section.tsx
  - src/components/sections/contact-form-section.tsx
  - src/components/sections/contact-info-section.tsx
  - src/lib/data/contact.ts
autonomous: true
requirements:
  - CAREER-01
  - CONTACT-01
  - CONTACT-02

must_haves:
  truths:
    - "User sees careers page with 3 job listings showing title, location, type, and description"
    - "User sees company values and benefits on the careers page"
    - "User sees an application email link (employment@kenexis.com) on the careers page"
    - "User can fill out a contact form with name, email, company, phone, message, and service interest dropdown"
    - "User sees a success message after submitting the contact form (demo mode, no real submission)"
    - "User sees phone (+1-614-451-7031) and email (info@kenexis.com) prominently on the contact page"
  artifacts:
    - path: "src/app/(marketing)/careers/page.tsx"
      provides: "Careers page with metadata"
      contains: "JobListingsSection"
    - path: "src/app/(marketing)/contact/page.tsx"
      provides: "Contact page with metadata"
      contains: "ContactFormSection"
    - path: "src/components/sections/job-listings-section.tsx"
      provides: "Job listing cards with title, location, type"
      min_lines: 40
    - path: "src/components/sections/company-culture-section.tsx"
      provides: "Company values and benefits display"
      min_lines: 30
    - path: "src/components/sections/contact-form-section.tsx"
      provides: "Demo contact form with all required fields"
      min_lines: 60
    - path: "src/components/sections/contact-info-section.tsx"
      provides: "Phone, email, address, fax display"
      min_lines: 30
    - path: "src/lib/data/contact.ts"
      provides: "Updated contactFormFields with phone and serviceInterest"
      contains: "serviceInterest"
  key_links:
    - from: "src/components/sections/contact-form-section.tsx"
      to: "src/lib/data/contact.ts"
      via: "contactFormFields import for form rendering"
      pattern: "contactFormFields"
    - from: "src/components/sections/contact-info-section.tsx"
      to: "src/lib/data/contact.ts"
      via: "contactInfo import for phone/email display"
      pattern: "contactInfo"
    - from: "src/components/sections/job-listings-section.tsx"
      to: "src/lib/data/careers.ts"
      via: "jobListings, applicationEmail imports"
      pattern: "jobListings"
---

<objective>
Build the Careers page (/careers) and Contact page (/contact) with demo contact form, installing shadcn textarea/label/select components.

Purpose: Complete the remaining Phase 9 pages so users can explore career opportunities and submit inquiries.
Output: 2 new pages with 4 new section components, updated contact data, 3 new shadcn components installed.
</objective>

<execution_context>
@C:/Users/geoca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geoca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-company-careers-contact/09-RESEARCH.md

@src/lib/data/careers.ts
@src/lib/data/contact.ts
@src/lib/data/types.ts
@src/components/sections/services-hero-section.tsx
@src/components/sections/contact-cta-section.tsx
@src/components/sections/newsletter-section.tsx
@src/app/(marketing)/training/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/data/careers.ts:
```typescript
export interface JobListing {
  id: string;
  title: string;
  location: string;
  type: "full-time";
  description: string;
}

export const jobListings: JobListing[];      // 3 jobs
export const companyValues: string[];         // 4 values
export const benefits: string[];             // 6 benefits
export const applicationEmail: string;        // "employment@kenexis.com"
export const careersSEO: SEOMeta;
```

From src/lib/data/contact.ts:
```typescript
export interface FormField {
  name: string;
  label: string;
  type: "text" | "email" | "tel" | "textarea" | "select";
  required: boolean;
  placeholder?: string;
  options?: string[];
  maxLength?: number;
}

export const contactInfo: ContactInfo;          // phone, email, address, fax
export const contactFormFields: FormField[];    // NEEDS UPDATE -- missing phone and serviceInterest
export const contactSEO: SEOMeta;
```

From src/components/sections/newsletter-section.tsx (demo form pattern):
```typescript
"use client";
const [submitted, setSubmitted] = useState(false);
// form onSubmit => e.preventDefault(); setSubmitted(true);
// disabled={submitted} on inputs and button
// {submitted && <p>Thank you message</p>}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components, update contact data, and build careers page</name>
  <files>
    src/lib/data/contact.ts,
    src/components/sections/job-listings-section.tsx,
    src/components/sections/company-culture-section.tsx,
    src/app/(marketing)/careers/page.tsx
  </files>
  <action>
1. Install shadcn components needed for the contact form:
```bash
npx shadcn@latest add textarea label select
```

2. Update src/lib/data/contact.ts -- replace the contactFormFields array to match CONTACT-01 requirement:
```typescript
export const contactFormFields: FormField[] = [
  {
    name: "name",
    label: "Name",
    type: "text",
    required: true,
    placeholder: "Your full name",
  },
  {
    name: "email",
    label: "Email",
    type: "email",
    required: true,
    placeholder: "your.email@company.com",
  },
  {
    name: "company",
    label: "Company",
    type: "text",
    required: false,
    placeholder: "Your company name (optional)",
  },
  {
    name: "phone",
    label: "Phone",
    type: "tel",
    required: false,
    placeholder: "+1 (555) 000-0000 (optional)",
  },
  {
    name: "serviceInterest",
    label: "Service Interest",
    type: "select",
    required: false,
    options: [
      "Process Hazards Analysis",
      "Quantitative Risk Analysis",
      "Fire & Gas Mapping",
      "Safety Instrumented Systems",
      "Software Products",
      "Training",
      "General Inquiry",
    ],
    placeholder: "Select a service area",
  },
  {
    name: "message",
    label: "Message",
    type: "textarea",
    required: true,
    placeholder: "Tell us about your project or question...",
    maxLength: 500,
  },
];
```
Keep all other exports (contactInfo, socialLinks, contactSEO) unchanged.

3. Create src/components/sections/job-listings-section.tsx:
- "use client" with motion/react
- Import jobListings, applicationEmail from @/lib/data/careers
- Import Card/CardHeader/CardTitle/CardDescription/CardContent from shadcn, Badge from shadcn
- Import Briefcase, MapPin, Mail icons from lucide-react
- Heading: "Open Positions" centered, with subtitle "Join our team of process safety experts"
- Grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 for the 3 job cards
- Each card:
  - CardHeader: CardTitle with job.title, CardDescription with flex items showing MapPin icon + job.location and Badge for job.type ("Full-time")
  - CardContent: job.description text
- Below the grid: "Apply" section with text "Send your resume to" and a styled mailto link to applicationEmail (Mail icon + email address, text-accent hover:underline)
- Use containerVariants + itemVariants stagger animation
- Section bg-background, py-16 sm:py-24, max-w-6xl mx-auto px-4

4. Create src/components/sections/company-culture-section.tsx:
- "use client" with motion/react
- Import companyValues, benefits from @/lib/data/careers
- Import CheckCircle, Heart icons from lucide-react
- Two-column layout on lg (grid-cols-1 lg:grid-cols-2 gap-12):
  - Left column: "Our Values" heading, list of companyValues with Heart icon + text for each
  - Right column: "Benefits" heading, list of benefits with CheckCircle icon + text for each
- Use motion.div whileInView animation
- Section bg-muted/50, py-16 sm:py-24, max-w-4xl mx-auto px-4

5. Create src/app/(marketing)/careers/page.tsx:
- Server component
- Export metadata using careersSEO
- Compose:
  1. ServicesHeroSection with title="Careers at Kenexis", subtitle="Build your career in process safety engineering", backgroundImage="/images/stock/hero-safety-equipment.jpg"
  2. JobListingsSection
  3. CompanyCultureSection
  4. ContactCTASection
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - shadcn textarea, label, and select components are installed
    - contactFormFields in contact.ts includes name, email, company, phone, serviceInterest (select), and message fields
    - /careers page renders with hero, 3 job listing cards, application email link, company values, benefits, and contact CTA
    - careersSEO metadata applied to page
  </done>
</task>

<task type="auto">
  <name>Task 2: Build contact page with demo form and contact info display</name>
  <files>
    src/components/sections/contact-form-section.tsx,
    src/components/sections/contact-info-section.tsx,
    src/app/(marketing)/contact/page.tsx
  </files>
  <action>
1. Create src/components/sections/contact-form-section.tsx:
- "use client" with useState from react, motion from motion/react
- Import Input from shadcn, Textarea from shadcn, Label from shadcn, Button from shadcn
- Import Select, SelectContent, SelectItem, SelectTrigger, SelectValue from shadcn
- Import contactFormFields from @/lib/data/contact
- Component renders a form with all fields from contactFormFields data:
  - For type "text", "email", "tel": render Label + Input with appropriate type attribute
  - For type "textarea": render Label + Textarea with maxLength
  - For type "select": render Label + Select with SelectTrigger/SelectContent/SelectItems from options array
  - Required fields get asterisk in label: `{field.label} {field.required && "*"}`
  - All inputs get disabled={submitted} prop
  - Layout: name and email on same row (grid-cols-1 sm:grid-cols-2 gap-4), company and phone on same row, serviceInterest full width, message full width
- Form onSubmit: e.preventDefault(), setSubmitted(true) -- demo mode, no backend
- Submit button: full width, bg-accent text-accent-foreground hover:bg-orange-600, text changes to "Message Sent!" when submitted
- Success message below form (matches newsletter pattern): "Thank you for your inquiry! We will respond within one business day."
- Heading: "Send Us a Message" centered above the form
- motion.div wrapper with whileInView fade-in animation
- Section bg-background, py-16 sm:py-24, max-w-2xl mx-auto px-4

2. Create src/components/sections/contact-info-section.tsx:
- "use client" with motion/react
- Import contactInfo from @/lib/data/contact
- Import Phone, Mail, MapPin, Printer icons from lucide-react
- 4-column responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8) centered text:
  - Phone: circular icon container (w-12 h-12 rounded-full bg-accent/10 inline-flex items-center justify-center), heading "Phone", tel: link with contactInfo.phone
  - Email: same icon container pattern, heading "Email", mailto: link with contactInfo.email
  - Address: same pattern, heading "Address", contactInfo.address text (no link, just text)
  - Fax: same pattern, heading "Fax", contactInfo.fax text
- Phone and email links: text-muted-foreground hover:text-accent transition
- motion.div whileInView fade-in animation
- Section bg-muted/50, py-16 sm:py-24, max-w-4xl mx-auto px-4

3. Create src/app/(marketing)/contact/page.tsx:
- Server component
- Export metadata using contactSEO
- Compose:
  1. ServicesHeroSection with title="Contact Us", subtitle="Get in touch with our process safety experts", backgroundImage="/images/stock/hero-control-room.jpg"
  2. ContactInfoSection (phone/email/address/fax prominently displayed -- CONTACT-02)
  3. ContactFormSection (demo form -- CONTACT-01)
  4. ContactCTASection is NOT needed here since this IS the contact page. Instead, omit or use a simple closing section.
- Note: Do NOT include ContactCTASection on the contact page (it says "Contact Us" and links to /contact, which would be self-referential). The page itself IS the contact destination.
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /contact page renders with hero, contact info grid (phone, email, address, fax), and demo contact form
    - Phone (+1-614-451-7031) and email (info@kenexis.com) are prominently displayed with icons (CONTACT-02)
    - Contact form has all 6 fields: name, email, company, phone, service interest dropdown, message (CONTACT-01)
    - Service interest uses shadcn Select component with 7 options
    - Form submission shows success message and disables all fields (demo mode)
    - contactSEO metadata applied to page
    - No self-referential ContactCTASection on the contact page
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Both routes render: /careers, /contact
- Navigation links to /careers and /contact work
- Contact form fields match CONTACT-01: name, email, company, phone, message, service interest dropdown
- Phone and email are visible on contact page (CONTACT-02)
- Careers page shows 3 job listings and application email
- Form demo submission works (success message appears, fields disable)
</verification>

<success_criteria>
- Careers page at /careers shows 3 jobs, values, benefits, and application email
- Contact page at /contact shows phone/email prominently and has a complete demo contact form
- Contact form has service interest dropdown using shadcn Select (not plain text input)
- All form fields from CONTACT-01 requirement are present
- Both pages follow established visual patterns (dark hero, alternating backgrounds, stagger animations)
</success_criteria>

<output>
After completion, create `.planning/phases/09-company-careers-contact/09-02-SUMMARY.md`
</output>
