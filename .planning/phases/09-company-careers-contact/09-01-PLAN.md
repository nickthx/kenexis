---
phase: 09-company-careers-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/about/page.tsx
  - src/app/(marketing)/about/team/page.tsx
  - src/app/(marketing)/about/representatives/page.tsx
  - src/components/sections/company-overview-section.tsx
  - src/components/sections/team-section.tsx
  - src/components/sections/leadership-preview-section.tsx
  - src/components/sections/representatives-section.tsx
  - src/lib/data/company.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "User can read the About page with company history, mission, industries, and regulatory alignment"
    - "User sees leadership preview (Edward Marszal and Kevin Mitchell) on the About page with photos and titles"
    - "User can click 'Meet Our Full Team' on About page and navigate to /about/team"
    - "User sees all 10 team members on /about/team with photos, names, titles, and credential badges"
    - "User can view /about/representatives with 11 representatives grouped by geographic region"
    - "User sees phone numbers, websites, and contact names for each representative"
  artifacts:
    - path: "src/app/(marketing)/about/page.tsx"
      provides: "Company overview page with metadata"
      contains: "ServicesHeroSection"
    - path: "src/app/(marketing)/about/team/page.tsx"
      provides: "Full team grid page with metadata"
      contains: "TeamSection"
    - path: "src/app/(marketing)/about/representatives/page.tsx"
      provides: "Representatives page with metadata"
      contains: "RepresentativesSection"
    - path: "src/components/sections/company-overview-section.tsx"
      provides: "Company info display with history, mission, industries"
      min_lines: 40
    - path: "src/components/sections/team-section.tsx"
      provides: "Full team member card grid"
      min_lines: 40
    - path: "src/components/sections/leadership-preview-section.tsx"
      provides: "Leadership-only preview with CTA to full team page"
      min_lines: 30
    - path: "src/components/sections/representatives-section.tsx"
      provides: "Representatives grouped by region with contact details"
      min_lines: 50
  key_links:
    - from: "src/app/(marketing)/about/page.tsx"
      to: "src/lib/data/company.ts"
      via: "aboutSEO import for metadata"
      pattern: "aboutSEO"
    - from: "src/components/sections/leadership-preview-section.tsx"
      to: "/about/team"
      via: "Link CTA to full team page"
      pattern: "href.*about/team"
    - from: "src/components/sections/representatives-section.tsx"
      to: "src/lib/data/company.ts"
      via: "representatives data import"
      pattern: "representatives"
---

<objective>
Build the About section: company overview page (/about) with leadership preview, full team page (/about/team), and representatives page (/about/representatives).

Purpose: Complete the company information section so users can learn about Kenexis, see the team, and find global representatives.
Output: 3 new pages with 4 new section components, all following established patterns.
</objective>

<execution_context>
@C:/Users/geoca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geoca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-company-careers-contact/09-RESEARCH.md

@src/lib/data/company.ts
@src/lib/data/types.ts
@src/components/sections/services-hero-section.tsx
@src/components/sections/contact-cta-section.tsx
@src/app/(marketing)/training/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/data/company.ts:
```typescript
export interface TeamMember {
  id: string;
  name: string;
  title: string;
  credentials: string[];
  photo: string;
  isLeadership: boolean;
}

export interface Representative {
  id: string;
  company: string;
  contactName: string | null;
  regions: string[];
  phone: string;
  email: string | null;
  website: string | null;
}

export interface CompanyInfo {
  name: string;
  founded: number;
  founders: string[];
  headquarters: ContactInfo;
  houstonOffice: string;
  description: string;
  mission: string;
  industries: string[];
  regulatoryAlignment: string[];
  socialLinks: SocialLink[];
}

export const aboutSEO: SEOMeta = { ... };
export function getLeadership(): TeamMember[] { ... }
// NOTE: representativesSEO and teamSEO DO NOT exist yet -- must be added
```

From src/lib/data/types.ts:
```typescript
export interface SEOMeta {
  title: string;
  description: string;
  ogImage?: string;
}
```

From src/components/sections/services-hero-section.tsx:
```typescript
interface ServicesHeroSectionProps {
  title?: string;
  subtitle?: string;
  backgroundImage?: string;
}
export function ServicesHeroSection(props: ServicesHeroSectionProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEO objects and build About page with company overview and leadership preview</name>
  <files>
    src/lib/data/company.ts,
    src/components/sections/company-overview-section.tsx,
    src/components/sections/leadership-preview-section.tsx,
    src/app/(marketing)/about/page.tsx
  </files>
  <action>
1. In src/lib/data/company.ts, add two new SEO exports after the existing aboutSEO:

```typescript
export const teamSEO: SEOMeta = {
  title: "Meet the Team - Kenexis Process Safety",
  description: "Meet the experienced process safety engineers and consultants at Kenexis Consulting Corporation.",
  ogImage: "/images/stock/about-hero.jpg",
};

export const representativesSEO: SEOMeta = {
  title: "Global Representatives - Kenexis",
  description: "Find Kenexis representatives worldwide. Local support for process safety consulting, software, and training across North America, South America, Europe, Middle East, Asia Pacific, and Africa.",
};
```

2. Create src/components/sections/company-overview-section.tsx:
- "use client" with motion/react for scroll animations
- Import companyInfo from @/lib/data/company
- Display companyInfo.description and companyInfo.mission as styled paragraphs
- Show "Founded {companyInfo.founded} by {companyInfo.founders.join(' and ')}" with Building2 icon
- Show companyInfo.industries as Badge components in a flex-wrap row
- Show companyInfo.regulatoryAlignment as Badge variant="outline" components
- Include companyInfo.headquarters address with MapPin icon
- Use alternating sections: company info on bg-background, industries/regulatory on bg-muted/50
- Apply motion.div whileInView fade-in animations (same pattern as all existing sections)
- Section padding: py-16 sm:py-24, max-w-4xl mx-auto

3. Create src/components/sections/leadership-preview-section.tsx:
- "use client" with motion/react
- Import getLeadership from @/lib/data/company
- Import Link from next-view-transitions, Button from shadcn
- Call getLeadership() to get the 2 leadership members
- Render each in a Card with centered layout:
  - next/image with fill prop in a w-28 h-28 rounded-full overflow-hidden container
  - sizes="112px", object-cover class
  - CardTitle with member.name, CardDescription with member.title
  - Badge components for each credential
- Below the leadership cards, add a "Meet Our Full Team" CTA:
  - Button asChild with Link href="/about/team"
  - Use variant="outline" so it doesn't compete with section CTA
- Use bg-muted/50 background with stagger animation (containerVariants + itemVariants pattern)

4. Create src/app/(marketing)/about/page.tsx:
- Server component (no "use client")
- Export metadata using aboutSEO (title, description, openGraph with ogImage)
- Compose sections in order:
  1. ServicesHeroSection with title="About Kenexis", subtitle="Process safety experts since 2004", backgroundImage="/images/stock/about-hero.jpg"
  2. CompanyOverviewSection
  3. LeadershipPreviewSection
  4. ContactCTASection
- Follow exact same pattern as training/page.tsx
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /about page renders with hero, company overview (description, mission, founded, industries, regulatory standards, address), leadership preview (2 members with photos), and contact CTA
    - "Meet Our Full Team" button links to /about/team
    - aboutSEO metadata applied to page
    - teamSEO and representativesSEO exports exist in company.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Build team page and representatives page with region grouping</name>
  <files>
    src/components/sections/team-section.tsx,
    src/components/sections/representatives-section.tsx,
    src/app/(marketing)/about/team/page.tsx,
    src/app/(marketing)/about/representatives/page.tsx
  </files>
  <action>
1. Create src/components/sections/team-section.tsx:
- "use client" with motion/react
- Import teamMembers from @/lib/data/company
- Import Image from next/image, Card/CardHeader/CardTitle/CardDescription/CardContent from shadcn, Badge from shadcn
- Render heading: "Meet Our Team" centered, with subtitle about the team size
- Render all 10 teamMembers in a responsive grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6
- Each team member card (same structure as leadership-preview-section):
  - Card with text-center layout
  - CardHeader with items-center: circular photo container (w-24 h-24 rounded-full overflow-hidden), Image fill with sizes="96px" and object-cover
  - CardTitle: member.name
  - CardDescription: member.title
  - CardContent: flex-wrap gap-1 justify-center with Badge variant="secondary" for each credential
  - Leadership members (isLeadership=true) get a subtle visual indicator: a small Badge with "Leadership" text above their name, or a ring-2 ring-accent on the photo container
- Use containerVariants + itemVariants stagger animation pattern
- Section bg-background with py-16 sm:py-24, max-w-6xl mx-auto px-4

2. Create src/components/sections/representatives-section.tsx:
- "use client" with motion/react
- Import representatives from @/lib/data/company
- Import Card components, Badge, and icons (Globe, Phone, ExternalLink, Mail) from lucide-react
- Define region groups as a constant map for display ordering:
```typescript
const regionGroups = [
  { name: "North America", filter: (r: Representative) => ["Texas, USA", "Canada", "Michigan/Midwest, USA"].some(region => r.regions.includes(region)) },
  { name: "South & Central America", filter: (r: Representative) => r.regions.some(region => ["Brazil", "Argentina", "Chile", "Trinidad", "Caribbean", "Latin America"].includes(region)) },
  { name: "Middle East & Central Asia", filter: (r: Representative) => r.regions.some(region => ["UAE", "Algeria", "India", "Iraq", "Azerbaijan"].includes(region)) },
  { name: "Europe", filter: (r: Representative) => r.regions.some(region => ["Turkey", "Bulgaria"].includes(region)) },
  { name: "Africa", filter: (r: Representative) => r.regions.some(region => ["South Africa"].includes(region)) },
  { name: "Asia Pacific & Oceania", filter: (r: Representative) => r.regions.some(region => ["South Korea", "Australia", "New Zealand"].includes(region)) },
];
```
- For each region group with matching representatives:
  - Render region name as h3 heading with Globe icon
  - Grid of representative Cards (grid-cols-1 md:grid-cols-2 lg:grid-cols-3)
  - Each card: company name (CardTitle), contactName if present (text-sm text-muted-foreground), region Badges (variant="outline"), phone with Phone icon and tel: link, website link with ExternalLink icon (anchor tag, target=_blank), email with Mail icon if present
- Stagger animations per region group
- Section bg-background with py-16 sm:py-24, max-w-6xl mx-auto px-4

3. Create src/app/(marketing)/about/team/page.tsx:
- Server component
- Export metadata using teamSEO
- Compose:
  1. ServicesHeroSection with title="Meet Our Team", subtitle="Experienced process safety engineers and consultants", backgroundImage="/images/stock/about-hero.jpg"
  2. TeamSection
  3. ContactCTASection

4. Create src/app/(marketing)/about/representatives/page.tsx:
- Server component
- Export metadata using representativesSEO
- Compose:
  1. ServicesHeroSection with title="Global Representatives", subtitle="Local support for process safety worldwide", backgroundImage="/images/stock/about-hero.jpg"
  2. RepresentativesSection
  3. ContactCTASection
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /about/team page renders with hero and grid of all 10 team members showing photos, names, titles, credentials
    - Leadership members visually differentiated from regular team members
    - /about/representatives page renders with representatives grouped by 6 geographic regions
    - Each representative shows company name, contact name (if available), regions, phone, and website link
    - All navigation links (/about, /about/team, /about/representatives) work without 404
    - Both pages have proper SEO metadata
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All three routes render: /about, /about/team, /about/representatives
- Navigation mega menu links to all three About sub-pages work
- Team photos render correctly with consistent sizing
- Representatives are visually grouped by region, not a flat list
- All scroll animations trigger on scroll
</verification>

<success_criteria>
- Company overview page at /about shows history, mission, industries, regulatory standards, and leadership preview
- Full team grid at /about/team shows all 10 members with photos and credentials
- Representatives page at /about/representatives shows 11 reps grouped by 6 regions with contact details
- All pages follow established visual patterns (dark hero, alternating backgrounds, stagger animations)
- All three navigation links from the About mega menu dropdown resolve correctly
</success_criteria>

<output>
After completion, create `.planning/phases/09-company-careers-contact/09-01-SUMMARY.md`
</output>
