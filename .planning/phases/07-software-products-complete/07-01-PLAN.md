---
phase: 07-software-products-complete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/software.ts
  - src/components/sections/product-comparison-section.tsx
  - src/app/(marketing)/software/[slug]/page.tsx
autonomous: true
requirements: [SOFT-05, SOFT-06, SOFT-07, SOFT-08, SOFT-09, SOFT-10]

must_haves:
  truths:
    - "User can read dedicated product pages for Arbor, Bowtie-Q, Open-Audit, Effigy, and KISS API with SaaS-style hero, features, integrations, pricing, and CTAs"
    - "User sees comparison tables on every software product page showing modern KISS capabilities versus traditional approaches"
    - "All seven product pages are accessible from the software landing page grid and mega menu navigation"
    - "Comparison tables are responsive -- table layout on desktop, stacked cards on mobile"
    - "Section backgrounds alternate correctly with the new comparison section inserted"
  artifacts:
    - path: "src/lib/data/software.ts"
      provides: "ComparisonRow interface and comparisonTable data for all 7 products"
      contains: "ComparisonRow"
    - path: "src/components/sections/product-comparison-section.tsx"
      provides: "Responsive comparison table section component"
      exports: ["ProductComparisonSection"]
      min_lines: 80
    - path: "src/app/(marketing)/software/[slug]/page.tsx"
      provides: "Updated route with ProductComparisonSection wired in"
      contains: "ProductComparisonSection"
  key_links:
    - from: "src/app/(marketing)/software/[slug]/page.tsx"
      to: "src/components/sections/product-comparison-section.tsx"
      via: "import and render with product.comparisonTable data"
      pattern: "ProductComparisonSection"
    - from: "src/components/sections/product-comparison-section.tsx"
      to: "src/lib/data/software.ts"
      via: "ComparisonRow type import"
      pattern: "ComparisonRow"
    - from: "src/lib/data/software.ts"
      to: "SoftwareProduct interface"
      via: "comparisonTable field on every product"
      pattern: "comparisonTable"
---

<objective>
Add comparison tables to all 7 software product pages, completing the remaining 5 product pages (Arbor, Bowtie-Q, Open-Audit, Effigy, KISS API) and adding the "Modern vs. Traditional" differentiator.

Purpose: The remaining 5 product pages already render via the dynamic [slug] route created in Phase 6. This plan extends the data layer with comparison table content, creates a responsive comparison section component, and wires it into the existing route -- completing all software product requirements.

Output: All 7 product pages display a responsive comparison table between features and integrations sections.
</objective>

<execution_context>
@C:/Users/geoca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geoca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-software-products-complete/07-RESEARCH.md
@.planning/phases/06-software-products-core/06-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/data/software.ts:
```typescript
export interface SoftwareProduct {
  id: ProductId;
  name: string;
  slug: string;
  trademark: string;
  tagline: string;
  description: string;
  pricing: PricingTier[];
  features: string[];
  integrations: string[];
  url: string;
  heroImage: string;
  icon: string;
  seo: SEOMeta;
}
```

From src/app/(marketing)/software/[slug]/page.tsx:
```typescript
// Current section order (to be modified):
<ProductHeroSection product={product} />
<ProductFeaturesSection features={product.features} productName={product.name} />
<ProductIntegrationsSection integrations={product.integrations} productName={product.name} />
<ProductDetailCTASection product={product} overview={kissOverview} />
<ContactCTASection />
```

From src/components/sections/product-features-section.tsx:
```typescript
// Background: bg-background (light)
// Uses: containerVariants + itemVariants with whileInView stagger pattern
```

From src/components/sections/product-integrations-section.tsx:
```typescript
// Background: bg-muted/50 (gray)
// Uses: same containerVariants + itemVariants stagger pattern
```

Motion stagger pattern used in all product sections:
```typescript
const containerVariants: Variants = {
  hidden: { opacity: 0 },
  visible: { opacity: 1, transition: { staggerChildren: 0.08 } },
};
const itemVariants: Variants = {
  hidden: { opacity: 0, y: 20 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.4, ease: "easeOut" } },
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend data layer with comparison table data for all 7 products</name>
  <files>src/lib/data/software.ts</files>
  <action>
    1. Add the ComparisonRow interface above the SoftwareProduct interface:
    ```typescript
    export interface ComparisonRow {
      aspect: string;
      traditional: string;
      modern: string;
    }
    ```

    2. Add the comparisonTable field to the SoftwareProduct interface:
    ```typescript
    comparisonTable: {
      title?: string;
      rows: ComparisonRow[];
    };
    ```

    3. Add comparisonTable data to ALL 7 products in the softwareProducts array. Use the comparison content from the RESEARCH.md file (07-RESEARCH.md has examples for Open-PHA, Arbor, and KISS API). For the remaining products, compose buyer-focused comparison rows following these guidelines:
       - Each product gets 5-6 comparison rows
       - "aspect" column uses plain business language (not engineering jargon)
       - "traditional" describes the old/legacy approach pain point
       - "modern" describes the KISS product's advantage
       - Focus on business outcomes: cost, time, risk, compliance, collaboration, data portability

    Comparison data for each product (use RESEARCH.md examples where available, compose the rest):

    **Open-PHA** (from research): Pricing Transparency, Data Portability, Platform Access, Updates & Maintenance, Collaboration, Integration

    **Vertigo**: SIL Verification Speed, Failure Rate Data, Proof Test Optimization, Documentation, Compliance Tracking, Integration

    **Arbor** (from research): Complexity Handling, Failure Rate Data, SIS Integration, Vulnerability Analysis, Sensitivity Analysis

    **Bowtie-Q**: Diagram Building, Risk Quantification, Revision Tracking, TMEL Compliance, Visualization, Reporting

    **Open-Audit** (dual-tier like Open-PHA): Pricing, Assessment Workflow, Evidence Management, Compliance Standards, Audit Tracking, Platform Access

    **Effigy**: Detection Coverage Analysis, 3D Modeling, Plume Simulation, Coverage Grading, Detector Optimization, Standards Compliance

    **KISS API** (from research): Data Access, Business Intelligence, Data Silos, Human Error Risk, Enterprise Integration

    IMPORTANT: Add the comparisonTable field and data to all 7 products in the SAME edit as the interface change, so TypeScript never has a partial type mismatch. Do NOT make comparisonTable optional.
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 7 softwareProducts entries have comparisonTable with 5-6 ComparisonRow entries each. TypeScript compiles with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProductComparisonSection component and wire into product route</name>
  <files>src/components/sections/product-comparison-section.tsx, src/app/(marketing)/software/[slug]/page.tsx</files>
  <action>
    **Part A: Create the ProductComparisonSection component**

    Create `src/components/sections/product-comparison-section.tsx` as a "use client" component:

    - Props: `productName: string`, `trademark: string`, `rows: ComparisonRow[]`, `title?: string`
    - Import ComparisonRow type from `@/lib/data/software`
    - Use the same motion stagger pattern (containerVariants + itemVariants) as all other product sections
    - Section heading: `{title || "Modern vs. Traditional"}` with subtitle "See how {productName}{trademark} compares to traditional approaches"
    - Desktop layout (hidden on mobile, shown md+): CSS grid with 3 columns (Aspect, Traditional Approach, {productName}{trademark})
      - Header row with uppercase tracking-wide labels, accent color for the product column header
      - Data rows with alternating bg-background striping (index % 2 === 0)
      - Traditional column text in muted-foreground, modern column in foreground font-medium
    - Mobile layout (shown on mobile, hidden md+): Stacked cards with border, each showing aspect as heading, traditional and modern as labeled lines
      - Traditional label in muted-foreground, product name label in accent color
    - Section background: `bg-muted/50` (matching existing gray sections)
    - Max width: `max-w-4xl` centered
    - Use `whileInView` viewport trigger with `once: true, margin: "-100px"`

    Follow the exact component pattern from 07-RESEARCH.md "Responsive Comparison Table Component Pattern" section -- it has the complete implementation. Adapt it directly.

    **Part B: Wire into the product detail route**

    Modify `src/app/(marketing)/software/[slug]/page.tsx`:

    1. Add import: `import { ProductComparisonSection } from "@/components/sections/product-comparison-section";`
    2. Insert ProductComparisonSection between ProductFeaturesSection and ProductIntegrationsSection:
    ```tsx
    <ProductHeroSection product={product} />
    <ProductFeaturesSection features={product.features} productName={product.name} />
    <ProductComparisonSection
      productName={product.name}
      trademark={product.trademark}
      rows={product.comparisonTable.rows}
      title={product.comparisonTable.title}
    />
    <ProductIntegrationsSection integrations={product.integrations} productName={product.name} />
    <ProductDetailCTASection product={product} overview={kissOverview} />
    <ContactCTASection />
    ```

    **Part C: Fix section background alternation**

    After inserting the comparison section (bg-muted/50), the integrations section (also bg-muted/50) will be adjacent with the same background. Fix this by changing the integrations section background:

    In `src/components/sections/product-integrations-section.tsx`, change `bg-muted/50` to `bg-background` so the visual rhythm becomes:
    - Hero (dark gradient)
    - Features (bg-background -- light)
    - Comparison (bg-muted/50 -- gray)
    - Integrations (bg-background -- light)  <-- CHANGED from bg-muted/50
    - CTA (bg-navy-900 -- dark)
    - Contact CTA (bg-navy-900 -- dark)
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx tsc --noEmit 2>&1 | head -5 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>ProductComparisonSection renders on all 7 product pages between features and integrations. Section backgrounds alternate correctly with no two adjacent sections sharing the same background. Build succeeds with all 7 static product pages generated.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with all 7 product slugs generating static pages
2. TypeScript compiles with zero errors (`npx tsc --noEmit`)
3. `npm run dev` -- manually visit /software/arbor, /software/bowtie-q, /software/open-audit, /software/effigy, /software/kiss-api to confirm comparison tables render
4. Verify desktop shows 3-column table layout, mobile shows stacked cards
5. Verify section background alternation: light > gray > light > dark pattern
</verification>

<success_criteria>
- All 7 software product pages render with hero, features, comparison table, integrations, pricing CTA, and contact CTA sections
- Comparison tables show 5-6 rows each with Traditional vs Modern columns
- Responsive layout: table on desktop, stacked cards on mobile
- Section backgrounds alternate correctly (no two adjacent sections with same bg)
- Build passes, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-software-products-complete/07-01-SUMMARY.md`
</output>
