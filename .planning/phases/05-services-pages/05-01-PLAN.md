---
phase: 05-services-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/services-hero-section.tsx
  - src/components/sections/services-grid-section.tsx
  - src/components/sections/service-detail-sections.tsx
  - src/components/sections/related-services-section.tsx
  - src/app/(marketing)/services/page.tsx
  - src/app/(marketing)/services/[slug]/page.tsx
autonomous: true
requirements: [SERV-01, SERV-02, SERV-03, SERV-04, SERV-05, SERV-06]

must_haves:
  truths:
    - "User sees a services landing page with a grid of all four service areas (PHA, QRA, Fire & Gas Mapping, SIS) with descriptions, icons, and links"
    - "User can click through to each of the four service detail pages via /services/pha, /services/qra, /services/fire-gas-mapping, /services/sis"
    - "User reads each service detail page with full description, methodology steps, sub-services, and deliverables"
    - "User sees related services cross-links at the bottom of each service detail page that navigate to other service pages"
    - "All services pages follow the established visual language: dark hero sections, scroll animations, alternating dark/light backgrounds, consistent typography"
  artifacts:
    - path: "src/app/(marketing)/services/page.tsx"
      provides: "Services landing page with SEO metadata"
      contains: "servicesSEO"
    - path: "src/app/(marketing)/services/[slug]/page.tsx"
      provides: "Dynamic service detail page with generateStaticParams and generateMetadata"
      contains: "generateStaticParams"
      exports: ["generateStaticParams", "generateMetadata"]
    - path: "src/components/sections/services-hero-section.tsx"
      provides: "Dark hero section for services pages with background image and gradient overlay"
      min_lines: 25
    - path: "src/components/sections/services-grid-section.tsx"
      provides: "4-card grid of service areas with icons, descriptions, and CTA links"
      min_lines: 40
    - path: "src/components/sections/service-detail-sections.tsx"
      provides: "Description, Methodology, SubServices, and Deliverables sections for detail pages"
      min_lines: 80
    - path: "src/components/sections/related-services-section.tsx"
      provides: "Cross-links to related services at bottom of detail pages"
      min_lines: 30
  key_links:
    - from: "src/app/(marketing)/services/page.tsx"
      to: "src/components/sections/services-hero-section.tsx"
      via: "import ServicesHeroSection"
      pattern: "ServicesHeroSection"
    - from: "src/app/(marketing)/services/page.tsx"
      to: "src/components/sections/services-grid-section.tsx"
      via: "import ServicesGridSection"
      pattern: "ServicesGridSection"
    - from: "src/app/(marketing)/services/[slug]/page.tsx"
      to: "src/lib/data/services.ts"
      via: "getServiceBySlug and serviceAreas"
      pattern: "getServiceBySlug"
    - from: "src/app/(marketing)/services/[slug]/page.tsx"
      to: "src/components/sections/related-services-section.tsx"
      via: "import RelatedServicesSection with filtered related services"
      pattern: "RelatedServicesSection"
    - from: "src/components/sections/services-grid-section.tsx"
      to: "/services/{slug}"
      via: "next-view-transitions Link to each service detail page"
      pattern: "Link href.*services/"
---

<objective>
Build the complete services section: a landing page with hero and 4-service grid, plus dynamic detail pages for all four service areas with full content sections and related services cross-links.

Purpose: Deliver SERV-01 through SERV-06 -- users can explore all Kenexis consulting services through a polished landing page and individual detail pages with cross-navigation.

Output: 6 new files creating the services landing page, dynamic detail route, and 4 reusable section components.
</objective>

<execution_context>
@C:/Users/geoca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geoca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-services-pages/05-RESEARCH.md

@src/lib/data/services.ts
@src/lib/data/types.ts
@src/lib/navigation-utils.ts
@src/components/sections/hero-section.tsx
@src/components/sections/pillars-section.tsx
@src/components/sections/contact-cta-section.tsx
@src/app/(marketing)/page.tsx
@src/app/(marketing)/layout.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/data/services.ts:
```typescript
export interface SubService {
  name: string;
  description: string;
}

export interface ServiceArea {
  id: string;
  name: string;
  slug: string;
  shortDescription: string;
  fullDescription: string;
  methodology: string[];
  subServices: SubService[];
  deliverables: string[];
  relatedServiceIds: string[];
  heroImage: string;
  icon: string;
  seo: SEOMeta;
}

export const serviceAreas: ServiceArea[] // 4 entries: pha, qra, fire-gas-mapping, sis
export function getServiceBySlug(slug: string): ServiceArea | undefined
export const servicesSEO: SEOMeta
```

From src/lib/data/types.ts:
```typescript
export interface SEOMeta {
  title: string;
  description: string;
  ogImage?: string;
}
```

From src/lib/navigation-utils.ts:
```typescript
export function getNavIcon(iconName?: string): LucideIcon | null
// Maps: "shield" -> Shield, "chart-bar" -> BarChart3, "flame" -> Flame, "cpu" -> Cpu
```

Established patterns from Phase 4:
- Section components are "use client" in src/components/sections/, composed by server page.tsx
- Motion stagger: containerVariants + itemVariants with whileInView viewport={{ once: true, margin: "-100px" }}
- Button asChild with Link (from next-view-transitions) for CTA routing
- Dark sections: "bg-navy-900 text-white", Light sections: "bg-background" or "bg-muted/50"
- Card grid: shadcn Card/CardHeader/CardTitle/CardDescription/CardContent
- Homepage hero uses -mt-16 to bleed behind header; interior pages do NOT use -mt-16
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create services landing page with hero and grid sections</name>
  <files>
    src/components/sections/services-hero-section.tsx
    src/components/sections/services-grid-section.tsx
    src/app/(marketing)/services/page.tsx
  </files>
  <action>
Create three files for the services landing page:

**1. `src/components/sections/services-hero-section.tsx`** — A "use client" component with dark hero for the services landing page.
- Use next/image with `fill` and `object-cover` for the background image at `/images/stock/services-hero.jpg`
- Dark gradient overlay: `bg-gradient-to-b from-navy-900/80 via-navy-900/60 to-navy-900/80`
- Shorter than homepage hero: `min-h-[300px] lg:h-[400px]` (NOT min-h-[500px] — interior pages should be shorter)
- Do NOT use `-mt-16` (only homepage extends behind header)
- Animated heading "Process Safety Consulting Services" and subheadline "Expert consulting across the full process safety lifecycle" using motion initial/animate (same pattern as hero-section.tsx)
- Import motion from "motion/react"

**2. `src/components/sections/services-grid-section.tsx`** — A "use client" component rendering 4 service area cards in a responsive grid.
- Accept `services: ServiceArea[]` prop (import type from `@/lib/data/services`)
- Use motion containerVariants + itemVariants stagger pattern (identical to pillars-section.tsx)
- Use `whileInView="visible" viewport={{ once: true, margin: "-100px" }}`
- Grid: `grid grid-cols-1 sm:grid-cols-2 gap-8 max-w-4xl mx-auto` (2x2 at sm+ for exactly 4 cards)
- Each card uses shadcn `Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardContent`
- Icon: use `getNavIcon(service.icon)` from `@/lib/navigation-utils` — returns the correct Lucide component
- Icon styling: `className="text-accent mb-2" size={40} strokeWidth={1.5}` (matches pillars pattern)
- CTA: `Button variant="outline" asChild` wrapping `Link href={/services/${service.slug}}` from `next-view-transitions`
- Section wrapper: `py-16 sm:py-24 bg-background` with centered heading "Our Services" and descriptive subtitle

**3. `src/app/(marketing)/services/page.tsx`** — Server component composing the landing page.
- Export `metadata` using `servicesSEO` from `@/lib/data/services` (same pattern as homepage page.tsx)
- Include `openGraph` with title, description, and images from `servicesSEO.ogImage`
- Import and render: `ServicesHeroSection` → `ServicesGridSection` (pass `serviceAreas`) → `ContactCTASection` (reuse from Phase 4)
- This is a Server Component — no "use client" directive
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Services landing page renders at /services with dark hero, 4-card grid with icons and links, and contact CTA
    - Each card shows service name, short description, icon, and "Learn More" link to /services/{slug}
    - SEO metadata exported correctly from page.tsx
  </done>
</task>

<task type="auto">
  <name>Task 2: Create service detail pages with all content sections and related services</name>
  <files>
    src/components/sections/service-detail-sections.tsx
    src/components/sections/related-services-section.tsx
    src/app/(marketing)/services/[slug]/page.tsx
  </files>
  <action>
Create three files for the service detail pages:

**1. `src/components/sections/service-detail-sections.tsx`** — A "use client" component file exporting multiple section components for detail pages. All sections accept appropriate props from `ServiceArea` data. Use motion `whileInView` animations throughout.

Export these named components:

- **`ServiceDescriptionSection`** — Props: `{ description: string }`. White background (`bg-background`). Renders the full description text in a readable max-width container (`max-w-3xl mx-auto`). Use `prose` styling or manual text-lg text-muted-foreground. Fade-in animation.

- **`ServiceMethodologySection`** — Props: `{ methodology: string[], serviceName: string }`. Muted background (`bg-muted/50`). Heading: "Our {serviceName} Methodology" (or "Our Approach"). Render methodology items as a numbered or bulleted list with stagger animation. Use containerVariants/itemVariants pattern. Each item: a visual step indicator (number or bullet) + text.

- **`ServiceSubServicesSection`** — Props: `{ subServices: SubService[] }`. White background (`bg-background`). Heading: "Specialized Services". Render sub-services as a 2-column card grid (`grid-cols-1 md:grid-cols-2 gap-6`). Each card shows sub-service name as CardTitle and description as CardDescription. Use shadcn Card components with stagger animation.

- **`ServiceDeliverablesSection`** — Props: `{ deliverables: string[] }`. Dark background (`bg-navy-900 text-white`). Heading: "Key Deliverables". Render deliverables as a list with check icons (use Lucide `CheckCircle2` or `Check`). Stagger animation. Icon color: `text-accent` (orange).

All sections use `py-16 sm:py-24` vertical padding and `mx-auto max-w-7xl px-4` container pattern.

**2. `src/components/sections/related-services-section.tsx`** — A "use client" component for cross-links (SERV-06).
- Props: `{ services: ServiceArea[] }`
- Return `null` if `services.length === 0`
- Muted background (`bg-muted/50`). Heading: "Related Services"
- Grid: `grid grid-cols-1 sm:grid-cols-2 gap-6 max-w-3xl mx-auto`
- Each related service rendered as a shadcn Card with:
  - Icon from `getNavIcon(service.icon)` with `text-accent mb-2 size={36} strokeWidth={1.5}`
  - CardTitle: service name
  - CardDescription: service short description
  - CardContent: `Button variant="outline" asChild` with `Link href={/services/${service.slug}}`
- Use motion stagger animation (containerVariants/itemVariants)

**3. `src/app/(marketing)/services/[slug]/page.tsx`** — Async Server Component with dynamic routing.

CRITICAL Next.js 16 pattern — `params` is a Promise:
```typescript
export async function generateMetadata({
  params,
}: {
  params: Promise<{ slug: string }>;
}): Promise<Metadata> {
  const { slug } = await params;
  // ...
}

export default async function ServiceDetailPage({
  params,
}: {
  params: Promise<{ slug: string }>;
}) {
  const { slug } = await params;
  // ...
}
```

Implementation:
- `generateStaticParams()`: return `serviceAreas.map(s => ({ slug: s.slug }))` — pre-renders all 4 pages
- `generateMetadata()`: await params, get service via `getServiceBySlug(slug)`, return title + description + openGraph from `service.seo`
- Page component: await params, get service, call `notFound()` (from `next/navigation`) if service is undefined
- Compute related services: `serviceAreas.filter(s => service.relatedServiceIds.includes(s.id))`
- Render sections in order:
  1. `ServicesHeroSection` — reuse the same hero component but this could also be a per-service hero. For simplicity, create a small inline hero section using the service name and short description as heading/subheading, with the service's `heroImage`. Use the same pattern: next/image fill + gradient overlay + white text. Height: `min-h-[300px] lg:h-[400px]`. OR reuse `ServicesHeroSection` if you add optional `title`/`subtitle` props. The cleanest approach: add `title?: string` and `subtitle?: string` optional props to `ServicesHeroSection` with defaults for the landing page text. Then pass `service.name` and `service.shortDescription` for detail pages.
  2. `ServiceDescriptionSection` — pass `service.fullDescription`
  3. `ServiceMethodologySection` — pass `service.methodology` and `service.serviceName`
  4. `ServiceSubServicesSection` — pass `service.subServices`
  5. `ServiceDeliverablesSection` — pass `service.deliverables`
  6. `RelatedServicesSection` — pass computed `relatedServices`
  7. `ContactCTASection` — reuse from Phase 4

Important: When making `ServicesHeroSection` accept optional title/subtitle props, update the component created in Task 1 to accept `title?: string` and `subtitle?: string` with defaults. The landing page passes nothing (uses defaults), detail pages pass service-specific text.
  </action>
  <verify>
    <automated>cd "C:/Users/geoca/Kenexis Website" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All four service detail pages render at /services/pha, /services/qra, /services/fire-gas-mapping, /services/sis
    - Each detail page shows: dark hero with service name, full description, methodology steps, sub-services grid, deliverables list, related services cross-links, and contact CTA
    - Related services section shows 2 linked cards per page (each service has 2 relatedServiceIds) that navigate to the correct service pages
    - Invalid slugs like /services/invalid show Next.js 404 page via notFound()
    - generateStaticParams pre-renders all 4 slugs at build time
    - generateMetadata produces correct SEO title and description per service
    - All sections have scroll-triggered fade-in/stagger animations matching Phase 4 visual language
    - Alternating section backgrounds: hero(dark) → description(white) → methodology(muted) → sub-services(white) → deliverables(dark) → related(muted)
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full services section:

1. **Build succeeds:** `npx next build` completes with zero errors, all 5 routes pre-rendered (/services, /services/pha, /services/qra, /services/fire-gas-mapping, /services/sis)
2. **Landing page:** /services shows hero + 4-card grid + contact CTA
3. **Detail pages:** Each /services/{slug} shows 7 sections in correct order with alternating backgrounds
4. **Cross-links:** Related services at bottom of each detail page link to correct sibling services (not self)
5. **SEO:** Each page exports correct metadata with title and description
6. **Animations:** All sections use motion whileInView scroll-triggered animations
7. **Navigation:** All internal links use next-view-transitions Link component
</verification>

<success_criteria>
- `npx next build` succeeds with /services and all 4 /services/[slug] routes pre-rendered
- Services landing page displays 4 service cards with icons, descriptions, and working links
- Each service detail page displays all content sections (description, methodology, sub-services, deliverables)
- Related services cross-links appear on every detail page and navigate to correct service pages
- Visual language matches Phase 4: dark heroes, scroll animations, alternating backgrounds, orange accents
</success_criteria>

<output>
After completion, create `.planning/phases/05-services-pages/05-01-SUMMARY.md`
</output>
